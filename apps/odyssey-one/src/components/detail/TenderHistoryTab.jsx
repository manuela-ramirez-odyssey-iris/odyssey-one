import PaneEmpty from './PaneEmpty'

export default function TenderHistoryTab() {
  return (
    <PaneEmpty
      message="No tender history available."
      hint="Tender history records will appear here once tenders are processed."
    />
  )
}

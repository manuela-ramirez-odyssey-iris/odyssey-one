# Normalization Tracker

> Tracks which components have been normalized via the `/normalize` routine.
> Used by future purge routines to know what's safe vs still in legacy state.

---

## Normalized Components

| Component | File | Text Utility | Date | Notes |
|---|---|---|---|---|
| Badge | `packages/ui/src/Badge.jsx` | `text-badge` (default variants), `text-label-xs-semibold` (metric variant) | 2026-04-27 (extended 2026-05-04, extended 2026-05-11) | Props: `variant`, `leftIcon`, `rightIcon`, `statusDot`. Variants: amber, blue, green, red, purple, gray, **notification** (added 2026-05-04), **metric** (added 2026-05-11). The `notification` variant is a complete preset: `--bittersweet-600` bg + `--text-inverse` text + circular shape (`--radius-full`, 4px symmetric padding, min 20×20). The `metric` variant is a value-display pill: `--badge-gray-bg` + `--text-primary` + `--radius-lg` (8px) + 1px / `--spacing-2` padding + `text-label-xs-semibold` (12/16/600) + tabular-nums + iconless. Iconless via `hasLeft/hasRight/hasDot && !isMetric` gating. **Bg via class `.badge-metric`** (not inline) so parent context can override — used by WidgetMetricRow to darken the pill on row hover. Symmetric padding-per-side via `getPadding(leftIcon, rightIcon, isDot, isMetric)`. Gray's icon override applies to both slots. Figma component set `213:27` has matching 8 variants (amber/blue/green/red/purple/gray/notification/metric). Metric variant `1858:296` cloned from gray, dot + icon slot frames removed inside the variant only. |
| SidebarButton (atom) | `packages/ui/src/SidebarButton.jsx` | — | 2026-04-28 | 40×40 button with 20px icon slot. Three states: Default (transparent bg, icon `--text-tertiary`), Hover (transparent bg, icon `--text-primary`), Selected (`--deep-sea-neutral-300` bg, icon `--text-primary`). Hover and Selected share the icon color (`--text-primary` / 900) — they differ only by background, semantic-driven (2026-04-29 update). Border radius `--radius-lg`. Transitions on color via `transition-colors duration-150` (matches `--transition-fast`). Props: `state` ('default' / 'hover' / 'selected', defaults to 'default'), `icon` (React node, mirrors Badge's icon-slot pattern). Renders a `<div>` so it can be wrapped by `<a>` (NavLink) or `<button>` without invalid HTML nesting. Figma component set: `514:2479` with three variants (`Property 1=Default/Hover/Selected`) and an `Icon#580:0` `INSTANCE_SWAP` property pointing to placeholder `512:2395`. Hover variant `573:2` and Icon swap property added 2026-04-28 via `use_figma`. Code Connect mapping at `packages/ui/src/SidebarButton.figma.tsx` (publish via `npx figma connect publish` from `packages/ui/`). |
| Sidebar (organism) | `apps/odyssey-one/src/components/layout/Sidebar.jsx` (app-local) | — | 2026-04-29 | 7 items in 2 groups. Top transactional: Home (House) / Orders (ClipboardList) / Shipments (Container) / Tracking (Route). Bottom master: Carriers (Truck) / User Management (UserCog) / Partners (Handshake). 64px sidebar width, 12px uniform padding (`--spacing-3`), 8px button gap (`--spacing-2`), 24px group-separator gap (`--spacing-6`) above + below the divider, 1px solid `--deep-sea-neutral-300` border-top on the bottom group. Composes `SidebarButton` from `@odyssey/ui`. NavLink for routed items (all 6 of them); plain `<button>` for `Partners` (no route yet). Drops legacy "Settings" entry. Order follows David's 2026-04-29 grooming (transactional-vs-master framing — see `domain-documentation/domains-overview.md`). Figma component at node `597:514`; horizontal padding, button gap, and group-separator gap bound to `Spacing/3`, `Spacing/2`, `Spacing/6` Figma variables (added to `4. Sizing` collection 2026-04-29). Top padding dropped from 76px (Figma drift to clear a hypothetical fixed navbar) to 12px uniform — the navbar is a flex-column sibling in `AppShell.jsx`, no overlap. **Stays app-local** (not in `@odyssey/ui`) because it imports `react-router-dom` and depends on the app's specific routes. |
| GlobalSearch (molecule) | `packages/ui/src/GlobalSearch.jsx` | — | 2026-04-30 (sizing reworked + Title mode added 2026-05-06; scope dropdown scrapped 2026-05-12) | **3-state component set** (`State=Default | Focused | Title`) at Figma `658:18`. Default + Focused = search mode (history nav + scope + input + clear). Title = title mode — renders a centered text instead of the searchbar (Inter Semi Bold 20/28, white) for context-aware swaps (e.g. when entering an edit mode, pairs with TrailNav Mode=Editor). New props: `mode` (`'search'` default \| `'title'`), `title` (TEXT, default `"Edit Dashboard"`, maps to Figma `Title` TEXT property). New typography tokens: `--font-size-xl` (20px), `--line-height-xl` (28px), `--font-weight-semibold` (600) — added to `5. Typography` collection in Figma + tokens.css. Code Connect: 3 `figma.connect()` blocks per State variant (Default/Focused → mode='search'; Title → mode='title'). *Updated 2026-05-12:* **scope dropdown scrapped** — the chevron + clickable scope pill are gone; scope is now a non-interactive labeled `<span>` showing the static category (default `"All"`). Code props `onScopeClick`, `dropdownOpen`, `dropdownIcon` removed. Figma `Dropdown icon` INSTANCE_SWAP component property removed; chevron deleted from both Default + Focused scope frames; scope padding rebalanced to symmetric L12 R12. Navbar consumer (`apps/odyssey-one/src/components/layout/Navbar.jsx`) dropped CATEGORIES list, dropdown panel, `categoryDropdownOpen` state. Difference between Default and Focused variants is now only the clear-X icon brightness (DSN/400 → DSN/200) tied to internal input-focus state. — Original sizing spec below: |
| GlobalSearch — search mode (sizing spec) | `packages/ui/src/GlobalSearch.jsx` (GlobalSearchSearch) | — | 2026-04-30 (sizing reworked 2026-05-06) | History nav (chevron-left/right, lg, DSN/400) + Searchbar wrapper (`flex: 1`, `min-width: 0`, the **whole component** is now bounded by `minWidth=590` / `maxWidth=900` props on the OUTER container — old searchbar-only `400/800` superseded so the dropdown stays auto-width and only the search field stretches/shrinks with available space, DSN/900 bg, Carolina Blue/400 border via `::after` pseudo-element so child backgrounds don't clip it, `--radius-lg`) + Search Scope (DSN/700 bg, DSN/600 right divider, "All" label DSN/400) + input + Circle-X clear (md, DSN/400). Internal `focused` state (input focus OR `dropdownOpen` prop): border 1→2px, scope text + dropdown chevron + circle-x shift to DSN/200. Hover ladder on scope/clear buttons: 400→200 idle, 200→100 focused. Clear button uses `onMouseDown preventDefault` so clicking it doesn't blur the input. Props: `scope`, `onScopeClick`, `dropdownOpen`, `dropdownIcon` (slot — maps to Figma `Dropdown icon` INSTANCE_SWAP), `value`, `onChange`, `onClear`, `onBack`, `onForward`, `placeholder`, `minWidth`, `maxWidth`. Placeholder color + focus outline suppression in `apps/odyssey-one/src/styles/components.css`. Figma component set: `658:18` with `State=Default/Focused` variants. Code Connect: `packages/ui/src/GlobalSearch.figma.tsx`. Used in `Navbar.jsx`. **Note:** the scope dropdown menu (categories list) is still inline in Navbar — see SHP-66. |
| OdysseyLogo (atom) | `packages/ui/src/OdysseyLogo.jsx` | — | 2026-05-04 | Inline SVG (172×24, viewBox 0 0 172 24). Two color groups: "Odyssey" wordmark (white in light, DSN/900 in dark) + "One" wordmark (Carolina Blue/400 both variants). Props: `variant` (`'light'` / `'dark'`, defaults to light), `width`, `height`. Figma source: variant component `Property 1=Light` (id 484:2264) + presumed Dark variant. Extracted from inline SVG in `Navbar.jsx`. |
| LeadNav (molecule) | `packages/ui/src/LeadNav.jsx` | — | 2026-05-04 | Hamburger button (36×36, padding 8 / `--spacing-2`, icon DSN/500, Lucide Menu lg) + logo slot, gap 16 (`--spacing-4`). Props: `logo` (React node — defaults to `<OdysseyLogo />`, maps to Figma `Logo` INSTANCE_SWAP), `onMenuClick`. Figma component: `639:564` with `Logo#1211:0` INSTANCE_SWAP (default = OdysseyLogo Light). Code Connect: `packages/ui/src/LeadNav.figma.tsx`. Used in `Navbar.jsx` replacing the inline hamburger + huge SVG. |
| Button (atom) | `packages/ui/src/Button.jsx` | — | 2026-05-05 (extended 2026-05-06 with Ghost, extended 2026-05-11 with revised ButtonLink ladder + arrow translate, extended 2026-05-12 with ButtonLink padding 0, **disabled spec re-normalized 2026-05-19**) | First foundational interactive atom. **4 variants** (Primary / Secondary / Outline / **Ghost**) × **3 sizes** (sm 32h / md 36h / lg 40h) × **disabled state**, plus optional left-side `icon` slot. Hover / pressed are **CSS-driven via `:hover` / `:active`** (only `disabled` is exposed as a React prop). Spec — Primary: bg DSN/900 + 1px DSN/500 stroke + white text + shadow-sm; hover bg DSN/600; pressed bg DSN/900 + DSN/400 text + inset shadow `0 1 3 black@40%` (no outer shadow); disabled DSN/300 + white text, no border. Secondary: white bg + DSN/300 border + DSN/700 text; hover DSN/50 + DSN/400 border; pressed DSN/100 + DSN/400 text + DSN/400 border, no shadow; disabled DSN/300 + white, no border. Outline (designed for dark surfaces): transparent + DSN/300 border + white text; hover white@10% + same border; pressed white@20% + DSN/200 text + DSN/400 border, no shadow; disabled DSN/300 + white, no border. **Ghost** (added 2026-05-06, also for dark surfaces — lighter visual weight than Outline): transparent + **no border** + white text + no shadow at idle; hover white@10%; pressed white@20% + DSN/200 text, no shadow; disabled DSN/300 + white + shadow-sm (matches universal disabled). Ghost = Outline minus the border. Asymmetric padding only with icon (`.btn--has-icon` modifier swaps to symmetric without). All values via tokens — `--font-size-sm/base`, `--line-height-sm/base`, `--icon-size-lg`, `--spacing-2/3/4`, `--radius-lg`, `--shadow-sm`, `--transition-fast`, `--border-focus`, full DSN scale, `--white`. Padding values 6 / 14 / 18 stay raw (no Spacing token at those values). **Usage rule: Secondary on light surfaces only — for dark surfaces, use Outline (heavier) or Ghost (lighter)**. Secondary's hover/pressed fills lose contrast on dark backdrops. *Updated 2026-05-12:* **ButtonLink padding 0 across all 3 states** — `.btn--link` overrides the size paddings (`.btn--sm/md/lg`) with `padding: 0` + `height: auto` since link is purely typographic (no surface to pad). The override block is placed AFTER `.btn--has-icon-right.btn--*` rules in components.css and includes all chained selectors (`.btn--link.btn--sm/md/lg`, `.btn--link.btn--has-icon`, `.btn--link.btn--has-icon-right`) so it wins same-specificity cascade. Figma side: padding zeroed on all 3 State variants in set `1895:7` (Idle `1838:7`, Hover `1894:7`, Pressed `1894:11`). itemSpacing stays per-variant (16 / 20 / 20) — the +4 on Hover/Pressed drives the arrow translate semantic. Figma component set `1307:333` — **48 variants** (4 × 3 × 4) with `Show icon` BOOLEAN, `Icon` INSTANCE_SWAP (default `placeholder-20` at `512:2395`, in `Icons Placeholder` — same master SidebarButton uses; mirrors the Badge/SidebarButton placeholder convention so the slot looks empty until the consumer swaps in their real icon), `Label` TEXT. **TextGroup compensation pattern** in Figma: each variant's auto-layout is `[Icon → gap 6 → TextGroup[Spacer(2px) → Label]] + asymmetric pl/pr`. The 2px Spacer keeps the label visually centered when `Show icon = false` (since Figma BOOLEAN can't drive padding). Code achieves the same visual via `.btn--has-icon`. Typography variables added to Figma collection 5 (Font Size/sm + base, Line Height/sm + base, Font Weight/medium). New `lucide/plus` master added in `Icons lg` (`1303:5`). Code Connect maps Variant/Size enums + State (Idle/Hover/Pressed → disabled:false, Disabled → disabled:true) + Show icon BOOLEAN + Icon INSTANCE_SWAP + Label TEXT. Replaces app-local `apps/odyssey-one/src/components/ui/Button.jsx` (deleted). 6 consumers wired: FilterPanel, TableControls, DocumentsTab, CostAllocationTab, RoutingGuideTab, NotesTab. 4 with-icon buttons migrated to `icon={<Icon size={20} />}` prop. |
| TrailNav (molecule) | `packages/ui/src/TrailNav.jsx` | — | 2026-05-04 (Editor mode added 2026-05-06) | Right-side navbar molecule (mirrors `LeadNav`). **2-variant component set** (`Mode=Profile | Editor`) at Figma `1565:648`. Profile = original (bell + badge + divider + avatar + name/role + chevron). Editor = global-edit context (2 Buttons + 2 icons), hides Profile when active. Editor mode props: `mode`, `showPrimaryButton` / `primaryButtonLabel` / `onPrimaryButtonClick` (default Cancel / Ghost lg), `showSecondaryButton` / `secondaryButtonLabel` / `onSecondaryButtonClick` (default Save / Outline lg), `showHelpIcon` / `helpIcon` / `onHelpClick` (default `<CircleHelp />`), `showRightIcon` / `rightIcon` / `onRightIconClick` (default `<X />`, INSTANCE_SWAP scoped to lucide/x + lucide/circle-question-mark via collection artboard `X / Help` on Icons page). Hidden icon slots render a 20×20 spacer `<span>` so layout doesn't collapse (matches Figma's static-size wrapper). Editor icon hover: DSN/500 → DSN/200 (mirrors bell hover ladder, code-only, `.trail-nav-editor-icon:hover` rule). Cancel uses the new Ghost Button variant (added same day). Code Connect: two `figma.connect()` blocks, one per Mode variant. — Original Profile spec below: |
| TrailNav — Profile mode (legacy spec) | `packages/ui/src/TrailNav.jsx` (TrailNavProfile) | — | 2026-05-04 | Right-side navbar molecule (mirrors `LeadNav`). Composes: bell button (32×32, Lucide Bell lg, DSN/500) with `<Badge variant="notification">` overlay (absolute, top:-6/left:16) + DSN/700 vertical divider + avatar slot (32×32, `--radius-lg`) + name (DSN/300, sm/medium) + role (DSN/400, xs/regular, line-height 12) + chevron (md, DSN/500, swaps Down→Up via `dropdownOpen`, or replaced via `chevron` slot prop). Props: `name`, `role`, `avatar` (React node), `notificationCount`, `showNotification` (default = `notificationCount > 0`, maps to Figma `Show notification` BOOLEAN), `chevron` (slot, maps to Figma `Chevron` INSTANCE_SWAP), `dropdownOpen`, `onNotificationClick`, `onProfileClick`. Layout gaps: bell↔divider `--spacing-4` (16px), divider↔profile `--spacing-5` (20px). Hover (code-only, deliberately not pushed to Figma): bell brightens 500→200; profile button brightens name 300→100, role 400→200, chevron 500→200. Figma component `639:562` (renamed from `User`) with 4 properties: Name (TEXT), Role (TEXT), Show notification (BOOLEAN, wired to Badge `visible`), Chevron (INSTANCE_SWAP, default `lucide/chevron-down`). The notification chip is a real instance of `Badge / Variant=notification` (1256:2), not a hand-built frame. All colors bound to `Deep Sea Neutral/*` + `Bittersweet/600` (via Badge variant). Avatar in Navbar consumer comes from `apps/odyssey-one/src/data/sso-mock.js` (currentUser.avatarUrl, name, role). Code Connect: `packages/ui/src/TrailNav.figma.tsx`. Used in `Navbar.jsx`. **Profile dropdown menu** (Account / Manage Users / Sign out) is still inline in Navbar — same future work as SHP-66. |
| Navbar (organism) | `packages/ui/src/Navbar.jsx` | — | 2026-05-06 (searchRef dropped 2026-05-12) | First normalized organism. Layout shell that composes `LeadNav` + `GlobalSearch` + `TrailNav` via slot props (`lead`, `search`, `trail`). Provides the surface (DSN/900 bg, padding `14 var(--spacing-6) 14 var(--spacing-4)`) and the `flex items-center justify-between` distribution — three sections sit at natural width, browser distributes via justify-between (LeadNav and TrailNav are equal-width so GlobalSearch is visually centered). `trailRef` slot prop attaches to the inner wrapper around the trail slot — for consumer-rendered profile dropdown's click-outside detection. (`searchRef` was retired 2026-05-12 when the GlobalSearch scope dropdown was scrapped — search slot no longer needs sibling dropdown content.) Figma component `1661:206` (renamed from "Header") with 3 nested instances all `isExposedInstance: true`: LeadNav (`639:564`), GlobalSearch (`658:18`), TrailNav (`1565:648` set, default Mode=Profile). Background bound to `Deep Sea Neutral/900`; padding-left bound to `Spacing/4`; padding-right bound to `Spacing/6`; padding-top/bottom stay raw at 14 (not in spacing scale). Code Connect: `packages/ui/src/Navbar.figma.tsx` — maps the 3 nested instances to slot props using `figma.instance(...)`. |
| IconButton (atom) | `packages/ui/src/IconButton.jsx` | — | 2026-05-07 | 24×24 circular surface for a single icon. White bg + `--shadow-base` (new dual-shadow, see Shadows below) + `--radius-full` + padding `--spacing-1`. Icon inherits `var(--text-secondary)` via `currentColor`; consumers can override by setting `color` on the button or wrapping the icon. Props: `icon` (ReactNode), `onClick`, `ariaLabel`, `className`, `...props` spread. Figma component `1711:297`: `Icon` INSTANCE_SWAP defaults to `placeholder-16` (`213:2`) per the icon-slot convention (no universal default). Used internally by `EntityChip` as the trailing add button (configured to `lucide/plus` md). Code Connect: `packages/ui/src/IconButton.figma.tsx`. *Updated 2026-05-07 (Session 21):* Now polymorphic — `<button>` when `onClick` is provided, `<span>` otherwise. **Full state palette added** (Secondary-button family minus idle border): Idle white / 1px transparent / `--shadow-base` / DSN/700 icon → Hover DSN/50 / 1px DSN/400 / `--shadow-base` / DSN/700 → Pressed DSN/100 / 1px transparent (border returns to invisible) / no shadow / DSN/400 icon. Only Hover shows a visible border; Idle & Pressed both keep a transparent 1px border so layout stays stable across all three states. Used inside EntityChip with `onClick` wired through the chip's `onAddClick` prop. **Figma master converted to a 3-variant set** at `1754:295` (Idle `1711:297` / Hover `1753:295` / Pressed `1753:297`), parented inside Small Buttons frame `1724:654`. Idle effect bound to `shadow/base` style (drift fix — was raw values before). Code Connect updated to point at the set ID. Icon color across Figma variants is uniform (per-instance INSTANCE_SWAP overrides don't survive cleanly — known icon-color-rule limitation; code handles it via `currentColor`). |
| PageHeader (molecule) | `packages/ui/src/PageHeader.jsx` | — | 2026-05-07 | Top-of-route H1 + flex shell. Title is 3xl/3xl semibold DSN/900 (uses new `--font-size-3xl=32` + `--line-height-3xl=32` + `--font-weight-semibold` tokens). Container: `flex items-center justify-between width=100%`. Right side is reserved for optional `children` (future actions; no consumer uses this slot today). Props: `title`, `children`, `className`, `style`, `...rest`. Figma component `1693:49` (renamed from "Header"): `Title` TEXT defaults to "Home". Title is domain-aware — set per route. Wired in `apps/odyssey-one/src/routes/shipments/ShipmentsRoute.jsx` (replaces the previous inline `<h1 className="text-3xl font-semibold" style={...}>Shipments</h1>` — `text-3xl` was 30px not on our scale, lineHeight 32 raw, marginBottom 25 raw). Bottom margin stays consumer-side via `style={{ marginBottom: 25 }}`. Code Connect: `packages/ui/src/PageHeader.figma.tsx`. |
| SectionHeader (molecule) | `packages/ui/src/SectionHeader.jsx` | — | 2026-05-07 | Two-row section header (Home domain — not used in Shipments). Row 1: H2 (2xl/2xl semibold DSN/900) on left + optional supporting text (sm/sm regular DSN/500) on right (typically a "Last update: ..." timestamp). Row 2: optional `leadingActions` + `trailingActions` slots distributed via justify-between; row only renders when at least one is provided. Container: `flex column gap=var(--spacing-8)`. New tokens used: `--font-size-2xl=24` + `--line-height-2xl=32` + `--font-weight-regular=400`. Props: `title`, `supportingText`, `leadingActions`, `trailingActions`, `className`, `style`, `...rest`. Figma component `1696:49` (renamed from "Subheader"): `Title` TEXT default "Welcome Amy!", `Supporting text` TEXT default "Last update: 04/24/2026 03:51 PM". Drift fixed: supporting text was bound to `IBM Plex Sans Regular` — switched to `Inter Regular` + bound to `Font/primary`. **Not yet wired into a consumer** — built ahead of Home view buildout. Code Connect: `packages/ui/src/SectionHeader.figma.tsx`. |
| SearchField (molecule) | `packages/ui/src/SearchField.jsx` | `text-label-sm-medium` (optional label) | 2026-05-12 (extended 2026-05-13 with optional label row + info icon) | 32h white-surface search input. Anatomy: 16px `lucide/search` icon (`--text-tertiary`) + `<input>` (`--text-primary`, placeholder `--text-placeholder`) + optional `lucide/circle-x` clear button (`--text-tertiary`) shown when the input has a value. Surface: `--white` bg, 1px `--border-default` (DSN/300), `--radius-md` (6), `--shadow-sm`, padding `6 var(--spacing-3)` (top/bottom 6 raw), gap `--spacing-2`. Placeholder color + focus outline suppression in `apps/odyssey-one/src/styles/components.css` (no visible focus ring — surface stays static). Props: `value`, `onChange`, `onClear`, `placeholder` (default `"Search"`), `className`, `...rest`. Figma component `1959:76` on Components-Molecules: `Placeholder` TEXT default `"Search"`. Used as the search section inside WidgetsLeftMenu (FILL width). Code Connect: `packages/ui/src/SearchField.figma.tsx`. *Updated 2026-05-12:* restyled to mirror the Shipments `TableControls` searchbar — surface is now `--bg-primary` (was `--white`); border is `1px solid --border-default` idle → **`2px solid --deep-sea-neutral-600`** on input focus (transitions via `--transition-fast`); search-icon color shifts `--text-placeholder` (idle) → `--text-tertiary` (focused). Input now forces `spellCheck=false`, `autoCorrect=off`, `autoCapitalize=off`. Internal `useState` tracks focus via the input's `onFocus`/`onBlur`. |
| MenuRow (molecule) | `packages/ui/src/MenuRow.jsx` | — | 2026-05-12 | 36h horizontal row used inside left-menu groups (MenuDropdown + WidgetsLeftMenu). Anatomy: label (`label/sm regular`, `--text-secondary`) + trailing 16px `lucide/grip-vertical` (`--text-tertiary`) that fades in on hover. Padding `var(--spacing-2) var(--spacing-4)`, gap `var(--spacing-3)`, justify-between. **States are CSS-driven** (no React `state` prop): hover bg `--deep-sea-neutral-100`, pressed bg `--deep-sea-neutral-200`. Grip opacity 0 → 1 on `:hover` OR when an ancestor sets `data-dragging="true"` (dnd-kit convention). Renders as `<div>` (not `<button>`) so consumers can drag from anywhere on the row via rest-spread of dnd-kit `listeners` + `attributes`. Props: `label`, `onClick`, `className`, `...rest`. CSS rules in `apps/odyssey-one/src/styles/components.css` (around lines 1309–1315) under `.menu-row` — REPLACED the old `.widgets-left-menu__item*` rules from the WidgetsLeftMenu refactor. Figma component set `1973:87` on Components-Molecules with 3 State variants: Default (`1973:77`), Hover (`1973:79`), Pressed (`1973:83`); `Label` TEXT property default `"Total Orders"`. Code Connect: `packages/ui/src/MenuRow.figma.tsx` — 3 `figma.connect()` blocks (one per State variant), all rendering the identical `<MenuRow label={label} />` snippet because state is CSS-only in code. |
| MenuDropdown (molecule) | `packages/ui/src/MenuDropdown.jsx` | — | 2026-05-12 | Collapsible group inside a left-menu. Vertical layout, padding-top `var(--spacing-3)`. Header 32h (`text-label-xs-medium-uppercase`, `--text-tertiary`, border-bottom `--border-subtle`, click toggles), trailing icon = `lucide/chevron-down` (expanded) / `lucide/chevron-right` (collapsed). **Composition slot via `children`** — items container unmounts entirely when `expanded === false` (no display:none, no animation). Props: `title`, `expanded` (default `true`), `onToggle`, `children`. Figma component `1981:79` on Components-Molecules: `Title` TEXT default `"Orders"`, `Expanded` BOOLEAN default `true`, `ContentSlot` SLOT. The SLOT is a designer-only manual workflow — programmatic instance-child manipulation on slots isn't supported by the Figma plugin API. Code Connect: `packages/ui/src/MenuDropdown.figma.tsx` — single `figma.connect()` mapping `Title` TEXT → `title`, `Expanded` BOOLEAN → `expanded`. SLOT children NOT mapped explicitly (mirrors Widget's pattern — slot content lives outside Figma component props). *Updated 2026-05-12:* header hover affordance added — new class `.menu-dropdown__header` shifts color `--text-tertiary` → `--text-primary` on `:hover`. The chevron icons dropped their inline `color` style and now inherit `currentColor` from the header button so they brighten in lockstep. CSS lives in `apps/odyssey-one/src/styles/components.css` near the `.menu-row*` block. |
| Widget (molecule, 5 variants) | `packages/ui/src/Widget.jsx` | `text-display-3xl-semibold`, `text-display-4xl-semibold`, `text-heading-lg-medium`, `text-label-sm-medium`, `text-label-xs-medium`, `text-label-sm-regular` | 2026-05-07 (extended 2026-05-11 with hover/pressed states, extended 2026-05-12 with 3xChart trail alignment + 2x single-data chart + **3xCta variant**) | Home dashboard widget. **4 variants** (`1x` 170w / `2x` 360w / `3x` 360w / `3xChart` 360w) selected via `variant` prop — same component, different layouts. Shell: white bg + DSN/300 1px border + `--radius-xl` (12) + `--shadow-sm` + `--spacing-6` padding. Header has optional `GripVertical` (showGrip BOOLEAN), optional domain-icon (slot — used in 2x as inline, in 3x/3xChart in a `widget__domain-icon-container` w/ `--carolina-blue-50` bg + `--radius-xl`), title, optional close X button. Content per variant: 1x = button (value + label + inline arrow); 2x = data-container + WidgetPieChart md; 3x = WidgetMetricRow list; 3xChart = chart-section (value + WidgetPieChart lg) + WidgetMetricRow legend. Footer: optional Button link `goToLabel` w/ `iconRight={<ArrowRight />}` when `onGoToClick` provided. *Hover/pressed states added 2026-05-11:* **WidgetContent 1x button** — label DSN/600 → hover DSN/700 → press DSN/900; inline arrow DSN/text-link → hover DSN/700 + `translateX(4px)` → press DSN/900 + `translateX(0)`. **Widget close X** — DSN/500 → hover DSN/900 → press DSN/400 (lift). **Widget grip** — DSN/500 → hover DSN/900 + `cursor: grab` → press DSN/400 + `cursor: grabbing`. All color transitions via `--transition-fast`. Figma component set `Widget` at `1825:7` (`Variant=1x|2x|3x|3xChart`), `WidgetContent` set at `1825:8` (matching variants). Code Connect: `packages/ui/src/Widget.figma.tsx`. *Updated 2026-05-12:* `.widget__chart-section` adds `justify-content: space-between` (drift fix — Figma always had `primaryAxisAlignItems: SPACE_BETWEEN` on the chart-section frame `1774:1703`). 2x widget consumer (Home.jsx) now passes single-segment `chartSegments` to match the new WidgetPieChart md spec — 2x = one data point. *Round 2 (same day):* Added **`chartTotal` prop** that forwards to WidgetPieChart `total`. 2x consumer passes `chartTotal={100}` so the chart shows a true 42% blue + 58% rest-ring split. Without it, the 42-value segment would fill the whole ring (sum-as-total fallback). *Round 3 (same day):* Added **`showChart` prop** (default `true`) — when `false`, the 2x donut is omitted entirely. **Use case:** stat-only 2x widgets that don't need a chart (e.g. User Management → "Users Enrolled: 142", or any single-number stat where a percentage would be meaningless). Layout still flexes left-aligned via `.widget__content--2x` `justify-content: space-between` (one item → naturally left). Figma side: new `Show chart` BOOLEAN on WidgetContent set `1825:8` (default true), bound to the WidgetPieChart instance `visible` on the 2x variant only — 1x / 3x have no chart node, 3xChart's chart stays mandatory. No new Figma variants added; the BOOLEAN is enough. *Round 4 (same day):* **New variant `3xCta`** added to both component sets (`1825:7` Widget → `1934:715`, `1825:8` WidgetContent → `1930:37`). New prop `ctaRows` (array of `{ icon, label, onClick }`). Shell overrides padding to `0` and gap to `0`; `.widget--3xCta .widget__header` adds its own `24/24/24/12` padding + `border-bottom: 1px solid var(--border-subtle)`. ButtonLink footer is omitted for 3xCta (guard: `variant !== '1x' && variant !== '3xCta'`). **Border migration:** the `.widget` shell rule moved from `--border-default` (DSN/300) to `--border-subtle` (DSN/200), and all 4 existing Widget Figma variants rebound their stroke to `Border/subtle`. Semantic distinction now codified: `--border-default` = input-like surfaces (badges, buttons, inputs); `--border-subtle` = card-like surfaces (widgets). *Updated 2026-05-12:* **editMode props added** — new boolean `editMode` (default `false`) + `onRemove` callback. When `editMode === true`: (1) the header `GripVertical` is forced visible regardless of `showGrip`, (2) an absolute top-right X close button (`.widget__edit-close`) renders and calls `onRemove` on click, (3) `data-edit-mode="true"` is set on the root element. CSS rule `.widget[data-edit-mode="true"] .btn--link { pointer-events: none }` disables footer ButtonLink interaction without changing opacity — **explicit anti-fade requirement** so the widget surface stays visually unchanged in edit mode (the X + grip are the only added affordances). Code-only behavior — Figma not updated this round. *Updated 2026-05-12 (round 3):* (1) **1x now accepts `domainIcon`** — `Header` function's `showInlineDomainIcon` matches `variant === '1x' \|\| variant === '2x'` (was just 2x); 1x renders `.widget__domain-icon` at lead between grip and title when consumer passes `domainIcon`. (2) **1x shell gap is `22px`** (overrides default `--spacing-3`) and **content is top-aligned** — `.widget__content--1x` was removed from the `margin-top: auto` list, so the 22px gap from header to content is preserved. (3) **Edit-mode X routed through header `onClose` slot** — `Widget.jsx` now uses `onClose={editMode ? onRemove : onClose}`. The X uses the existing `.widget__close` styling (vertically centered with the rest of the header via `align-items: center`). The `.widget__edit-close` absolute-positioned layer and its CSS rules were removed entirely (no more z-index overlay). (4) **Edit-mode click-disable extended** from `.btn--link` only to **`.widget__content` and all descendants** — `.widget[data-edit-mode="true"] .btn--link, .widget[data-edit-mode="true"] .widget__content, .widget[data-edit-mode="true"] .widget__content * { pointer-events: none }`. The header `.widget__close` is outside `.widget__content` so it stays clickable (intended — X must still work in edit mode). (5) **Figma 1x master (`1774:1483`):** `primaryAxisAlignItems` SPACE_BETWEEN → MIN; `itemSpacing` 16 → 22. New `lucide/triangle-alert` (then swapped to `placeholder-20`) instance inserted at lead position in Header title (between grip and title). Title container set to `layoutSizingHorizontal=FILL` with text-truncation `ENDING`. (6) **Figma 2x master:** `clipboard-list` icon moved from the right-side "Button group" frame (next to X) to the "Header title" frame at lead (between grip and title text). The "Button group" frame now contains only the X icon. (7) **All Widget domain-icon Figma defaults → `placeholder-20`** (`512:2395` in Icons Placeholder) — 1x: `lucide/triangle-alert` → `placeholder-20`; 2x: `lucide/clipboard-list` → `placeholder-20`; 3x and 3xChart: `lucide/container` → `placeholder-20`. The Widget set's `Domain icon#1828:5` INSTANCE_SWAP property default also updated from `lucide/container` (`583:415`) to `placeholder-20` (`512:2395`). Follows the icon-slot convention — switchable INSTANCE_SWAP defaults to placeholder, never a real icon. 3xCta unaffected (no domain icon slot). |
| WidgetMetricRow (molecule) | `packages/ui/src/WidgetMetricRow.jsx` | `text-label-sm-regular`, `text-label-xs-semibold` (inherited via Badge metric) | 2026-05-07 (extended 2026-05-11 with hover/pressed states + inner Badge metric integration) | Single data row inside Widget 3x / 3xChart content: optional indicator dot + label + inline value Badge (metric variant) + trailing chevron. Polymorphic — renders as `<button>` when `onClick` provided, `<div>` otherwise. Layout: HORIZONTAL flex, `justify-content: space-between`. Label group: optional 8×8 indicator dot (`--radius-full`, default fill `--chart-1`, consumer overrides via `indicatorColor` prop) + label text. Trailing: inline `<Badge variant="metric">{value}</Badge>` + chevron-right (md, `--text-tertiary`). Replaces the prior inline `.widget-metric-row__badge` frame-styled-like-component with a real Badge instance (the Session 22 inline-pill normalization). *Hover/pressed states added 2026-05-11:* label `--text-secondary-soft` → hover `--text-primary` → press `--text-placeholder` (lift); chevron `--text-tertiary` → hover `--text-primary` → press `--text-placeholder` (lift); indicator scale(1) → hover scale(1.25) → press scale(1) (back to default — "build up on hover, release on press"); **inner `.badge-metric` bg `--badge-gray-bg` → hover `--deep-sea-neutral-200` (one step darker) → press `--badge-gray-bg`** — scoped to `.widget-metric-row--interactive:hover .badge-metric`, so standalone Badge metric instances elsewhere stay unaffected. Figma component `1814:7`. Code Connect: `packages/ui/src/WidgetMetricRow.figma.tsx`. |
| WidgetCtaRow (molecule) | `packages/ui/src/WidgetCtaRow.jsx` | `text-label-sm-medium` | 2026-05-12 | CTA link row used inside Widget 3xCta content. Anatomy: icon container 40×40 (`--carolina-blue-50` bg, `--radius-pill`, contains 20px lucide icon colored `--text-link` via `currentColor`) + 12px gap + label (`text-label-sm-medium`, `--text-primary`) + trailing chevron-right 16px (`--text-secondary-soft`). Polymorphic — `<button>` when `onClick` provided, `<div>` otherwise. Row 72h, `padding: 0 var(--spacing-4)`, `border-bottom: 1px solid var(--border-subtle)` with `:last-child { border-bottom: none }`. *Hover/pressed states:* row bg `--white` → `--deep-sea-neutral-50` (hover) → `--deep-sea-neutral-100` (press); icon-container bg `--carolina-blue-50` → `--carolina-blue-100` (#DCE8F7, NEW primitive added 2026-05-12) → CB/100 stays through press; chevron `--text-secondary-soft` → `--text-primary` → `--text-placeholder` (lift). All transitions `--transition-fast`. Figma master `1927:84` (Components-Molecules); single-state master per WidgetMetricRow pattern. Props: `Label` TEXT (default "Track a Shipment"), `Icon` INSTANCE_SWAP (default `placeholder-20` at `512:2395`). Code Connect: `packages/ui/src/WidgetCtaRow.figma.tsx`. |
| WidgetPieChart (molecule) | `packages/ui/src/WidgetPieChart.jsx` | `text-label-sm-medium` (center text) | 2026-05-11 (extended 2026-05-12: md single-segment + `total` prop + chart-grow easing) | SVG donut chart used inside Widget 2x (md=72) and Widget 3xChart (lg=96). 4 segments rendered as conic-style arcs with `stroke-dasharray` + rotation; "rest" ring bound to `--chart-rest`. Segments grow in on mount: `stroke-dasharray` transitions from `0 circumference` → final length over 800ms ease-out, driven by `useState`+`useEffect`+`requestAnimationFrame`. Whole chart scales `transform: scale(1.05)` on `:hover` via CSS. Props: `segments` (array of `{ value, color }`, color is a Chart/* token), `centerText` (optional, renders as `<span className="widget__pie-center text-label-sm-medium">`), `size` (`'md'` | `'lg'` | number — md=72, lg=96, raw number passes through). Stroke width = 18% of size. Figma component set `WidgetPieChart` at `1881:77` (`Size=md|lg` VARIANT + `Show center text` BOOLEAN + `Center text` TEXT). Replaces the inline donut SVGs previously rendered in WidgetContent2x and WidgetContent3xChart — Figma masters now reference `WidgetPieChart` instances (`isExposedInstance: true` so center-text props bubble up). Code Connect: `packages/ui/src/WidgetPieChart.figma.tsx`. *Updated 2026-05-12:* **md variant now renders ONE segment + rest ring** (was 4 segments). Variant semantic: md = 2x widget single-data view (one metric, one filled arc); lg = 3xChart widget multi-data view (4 filled arcs). Code accepts an array of any length — md consumers (Widget 2x in Home.jsx) pass `[{ value: 42, color: 'var(--chart-1)' }]`. Figma master `1880:78` now contains only Rest ring + Segment 1 + Center text (Segments 2/3/4 deleted). *Round 2 (same day):* Added **`total` prop** to WidgetPieChart so 2x consumers can pass `total={100}` and have the 42-value segment render as 42% of the ring (rest 58% as Chart/rest). Without `total`, segments still sum to the implicit total (preserves lg / 3xChart usage). Widget.jsx forwards via new `chartTotal` prop. Figma master Segment 1 arcData updated to a clean 42% arc from 12 o'clock going clockwise (startingAngle=−π/2+sweep, endingAngle=π/2, sweep=0.42·2π). **Grow-in animation easing** changed from `800ms ease-out` to **`--transition-chart-grow` token (`1000ms cubic-bezier(0.22, 1, 0.36, 1)` — ease-out-quart)** so the segment sweep reads as designed motion. Token lives in tokens.css alongside the existing `--transition-fast/base/slow` family. |
| WidgetsLeftMenu (organism) | `packages/ui/src/WidgetsLeftMenu.jsx` | — | 2026-05-12 | 240w side panel for browsing/searching widget metrics in Home edit mode. **Anatomy:** title row (48h, label/xs semibold `--text-primary`, border-bottom `--border-subtle`) + Search section (`<SearchField fill />` filtering items by `includes`, case-insensitive) + 6 collapsible groups. **Group header** (32h): `text-label-xs-medium-uppercase` (12/16/500 + `letter-spacing: var(--letter-spacing-wide)` 0.05em), `--text-tertiary`, border-bottom `--border-subtle`, trailing 16px chevron-down/right swapping on collapse. **Item row** (36h): label/sm regular `--text-secondary`; hover bg `--deep-sea-neutral-100`; pressed bg `--deep-sea-neutral-200`; 16px `lucide/grip-vertical` (`--text-tertiary`) fades in on hover (opacity 0 → 1, `--transition-fast`). Surface: `--white` bg, `--shadow-panel` (3-layer drop shadow, new token). Internal state: `collapsedGroups` Set + `query` string. **Library-pure** — no `@dnd-kit` import. Accepts `renderItem(item, group, defaultNode)` render-prop so consumers can wrap the default row in their own DnD adapter (the Home edit-mode consumer wraps each row in a Sortable). Props: `title` (default `"Metrics library"`, maps to Figma `Title` TEXT), `groups` (array of `{ title, items }`), `renderItem`, `className`, `...rest`. New tokens: `--letter-spacing-wide`, `--shadow-panel`, `--transition-panel`; new utility class `.text-label-xs-medium-uppercase` in `apps/odyssey-one/src/styles/components.css`. Figma component `1961:393` (Components-Organisms). Code Connect: `packages/ui/src/WidgetsLeftMenu.figma.tsx`. *Updated 2026-05-12 (same day):* organism refactored to compose `MenuRow` + `MenuDropdown` internally instead of inlining the row + group rendering. Removed `GripVertical` / `ChevronDown` / `ChevronRight` / `ICON_MD` imports. The `renderItem(item, group, defaultNode)` render-prop signature is unchanged — `defaultNode` is now a `<MenuRow>` element. Home's `SortablePanelItem` continues to work (`data-dragging="true"` on the wrapper drives `.menu-row` styles via the ancestor selector). CSS rules renamed from `.widgets-left-menu__item*` to `.menu-row*`. |
| ModalLarge (organism) | `packages/ui/src/ModalLarge.jsx` | `text-label-sm-semibold` (title), `text-label-xs-regular` (subtitle) | 2026-05-12 | Reusable large-modal shell. Header (title + optional subtitle + close X) / content slot (children) / footer slot (footer prop). ESC + overlay-click dismiss; dialog click stops propagation. `role="dialog"`, `aria-modal="true"`. Fixed 496px width; height hugs content. Tokens: `--bg-primary` modal bg, `--radius-xl` (12), `--shadow-md`, `--overlay-bg` (rgba(0,0,0,0.4)), `--border-subtle` header divider, header padding `--spacing-3 --spacing-3 --spacing-2 --spacing-4` (8px bottom per spec round 2), footer padding `--spacing-3 --spacing-4 --spacing-4 --spacing-4`. Close button: 40×40, `--radius-full`, transparent idle → `--deep-sea-neutral-100` hover → `--deep-sea-neutral-200` active. Props: `title`, `subtitle`, `showSubtitle` (default true), `onClose`, `children` (content slot), `footer` (footer slot), `className`, `ariaLabel`. **New text style** `label/sm semibold` created in Figma + matching `.text-label-sm-semibold` utility class added to `apps/odyssey-one/src/styles/components.css` — bound to `Font/primary`, `Font Size/sm`, `Line Height/sm`, `Font Weight/semibold`. Figma component `2006:663` on Components-Organisms (parented inside `Modals` artboard `1997:434`). Properties: `Title` TEXT, `Subtitle` TEXT, `Show subtitle` BOOLEAN (default true), `Content` SLOT, `Footer` SLOT — SLOT defaults: WidgetVariantPicker (1x) + Button (Primary, lg, "Insert widget"). Code Connect: 1 mapping at `packages/ui/src/ModalLarge.figma.tsx`. Consumer: Home edit-mode panel-item click opens this modal with `WidgetVariantPicker` inside (replaces the prior `ComingSoonModal` stub, which was deleted this session). |
| ModalMedium (organism) | `packages/ui/src/ModalMedium.jsx` | `text-heading-lg-semibold` (title — NEW utility), `text-label-sm-medium` (used by inner SearchField label) | 2026-05-13 | 540-wide reusable modal shell. Header (title + close X — no subtitle) / content slot (children) / footer slot (footer prop). ESC + overlay-click dismiss; dialog click stops propagation; `role="dialog"`, `aria-modal="true"`. Tokens: `--bg-primary` modal bg, `--radius-xl` (12), **`--shadow-2xl` (NEW token `0 25px 50px -12px rgba(0,0,0,0.25)`)**, `--border-subtle` footer divider, header padding `--spacing-6 --spacing-6 --spacing-3 --spacing-6`, content padding `--spacing-3 --spacing-6 0 --spacing-6` (bottom 0 so list sits flush to footer), footer padding `--spacing-3 --spacing-6`. Close button: 40×40, `--radius-lg` (rounded-square hover, NOT pill), `margin-right: calc(-1 * --spacing-2)` to optically align the X glyph with the modal's right edge, hover bg `--deep-sea-neutral-100`. Props: `title`, `onClose`, `children`, `footer`, `className`, `ariaLabel`. Figma component `2032:915` on Components-Organisms (inside `Modals` artboard). Properties: `Title` TEXT, `Content` SLOT, `Footer` SLOT. SLOT defaults: labeled SearchField (`Show label=true, Show info icon=true, Label="Add Customers", Placeholder="Search Customers"`) + 3 CustomerRow instances (first `Favorite=True`) for the content slot; Cancel (Secondary lg) + Save (Primary lg) for the footer slot. Nested-instance properties are intentionally NOT exposed — designers select each sub-instance directly (per user preference to keep the parent property panel clean given lists may grow to ~100 items). New text style `heading/lg semibold` created in Figma + matching `.text-heading-lg-semibold` utility class added to components.css. New Figma Effect Style `shadow/2xl` mirrors the `--shadow-2xl` token. Code Connect: 1 mapping. Consumer: Home `Customers` chip's `+` button opens this modal with `CustomerRow` list. |
| CustomerRow (molecule) | `packages/ui/src/CustomerRow.jsx` | `text-label-sm-medium` (label) | 2026-05-13 (extended 2026-05-18) | 48h selector row used inside the Add Customers modal (reusable for other entity-picker lists). Anatomy: 24×24 icon container (2px solid `--border-default` ring, `--radius-full`, `--bg-primary` bg, **padding 0** so the 16px lucide icon sits with just 2px gap each side from the ring) + label (`text-label-sm-medium`, `--text-secondary`) + favorite Star (lg, fill="currentColor" when favorite) + Trash2 (lg). Row padding `--spacing-3`, border-bottom `--border-subtle` between rows; `:last-child { border-bottom: none }` clears the trailing border. Action buttons (star + trash) share a color ladder: `--text-secondary` idle → `--text-primary` hover → `--text-placeholder` active. Polymorphic icon — `icon` prop overrides the default `<Handshake>` (other entity types: carriers/users/etc.). Props: `label`, `icon`, `favorite` (default false), `onFavoriteToggle`, `onDelete`, `className`. Figma component set `2029:461` on Components-Molecules with `Favorite=False | True` VARIANT (drives the star fill via inner Vector override). `Label` TEXT + `Icon` INSTANCE_SWAP (default `placeholder-16` per icon-slot convention) properties. Code Connect: 2 mappings (one per Favorite variant). |
| WidgetVariantPicker (organism) | `packages/ui/src/WidgetVariantPicker.jsx` | `text-label-xs-regular` (variant label) | 2026-05-12 (reclassified molecule → organism + moved to Modals artboard 2026-05-13) | Carousel-style widget-variant chooser used inside the Home edit-mode configurator modal. **4 variants** (`1x` / `2x` / `3x` / `3xChart`) — chooses which Widget shape the user inserts. Anatomy: left chevron arrow + info column (centered Widget instance + dots-container with variant label + 4 dots) + right chevron arrow. **Infinite wrap**: `goPrev`/`goNext` use modulo arithmetic — arrows never disable; reaching the end loops back. Tokens: `--bg-tertiary` (DSN/100) picker bg, 460px fixed height, padding `--spacing-3 --spacing-4`. Arrow buttons 40×40 `--radius-full` transparent idle → `--deep-sea-neutral-200` hover (text → `--text-primary`) → `--deep-sea-neutral-300` active. Dots 8×8 `--radius-full` (`--deep-sea-neutral-900` active / `--deep-sea-neutral-300` inactive / `--deep-sea-neutral-500` inactive-hover). Variant labels: `Small / Wide / Tall / Tall with chart`. Preview pointer-events disabled via `.widget-variant-picker__main { pointer-events: none }` — widget renders fully (Go-to link, 1x button, 3x metric rows) but nothing inside the preview registers clicks. **Scale-down**: variants `3x` and `3xChart` apply `transform: scale(0.85)` to the inner widget via `.widget-variant-picker[data-variant="3x" | "3xChart"] .widget` so the 360×386 widget breathes inside the 496-wide modal; 1x and 2x stay at 100%. DOM dimensions stay native — real Home grid is unaffected. Props: `variant` (default `'1x'`), `onVariantChange(next)`, `widgetProps` (forwarded to inner `<Widget />`), `className`. Figma component set `2005:554` on Components-Organisms (inside Modals artboard `1997:434`, moved from Components-Molecules 2026-05-13) (`Variant=1x|2x|3x|3xChart`). Nested Widget instance per variant is `isExposedInstance: true` so designers can override `Title` / `Domain icon` / `Show grip` at the picker instance level. Code Connect: 4 mappings (one per Variant) at `packages/ui/src/WidgetVariantPicker.figma.tsx` — all 4 render identical JSX with the right `variant` prop. Consumer: ModalLarge's content slot, fed via `previewWidgetProps(variant, label)` in `apps/odyssey-one/src/routes/Home.jsx` so each preview reads as a real widget with realistic per-variant fake data. |
| IconButtonGhost (atom) | `packages/ui/src/IconButtonGhost.jsx` | — | 2026-05-18 | 28×28 transparent-at-rest icon-only button with hover (DSN/100) + pressed (DSN/200) surface fills. Radius/lg. Polymorphic: `<button>` when `onClick` provided, else `<span>` (decorative). Inverse pattern to circular shadowed IconButton — IconButton has a visible surface at rest; IconButtonGhost is invisible until interacted. Replaces 3 prior inline implementations: `.modal-large__close`, `.modal-medium__close`, `.widget__close` (all CSS rules removed). Figma set `2138:304` on Components-Atoms (Small Buttons artboard) with State=Idle/Hover/Pressed variants. INSTANCE_SWAP `Icon` property defaulting to `placeholder-20`. Code Connect: `packages/ui/src/IconButtonGhost.figma.tsx`. |
| EmptyState (atom) | `packages/ui/src/EmptyState.jsx` | `text-label-xs-regular` (message) | 2026-05-18 | Centered icon over a help message on a DSN/100 surface, Radius/lg. Use when a list, search result, or data view has no content yet. Atom owns: padding `--spacing-6`, gap `--spacing-2`, flex column center, background, radius, color (`--text-placeholder`), text-align center. Margins are consumer-side (Home's Add Customers modal uses `.home-customers-empty` for `margin-top: --spacing-3` / `margin-bottom: --spacing-8`). Props: `icon` (React node), `message` (string), `className`. Figma `EmptyState` at `2159:295` on Components-Atoms (**new Panels artboard `2159:294`**). INSTANCE_SWAP `Icon` defaulting to `placeholder-20`; TEXT `Message` defaulting to "No items to show yet.". Code Connect: `packages/ui/src/EmptyState.figma.tsx`. |
| AddSectionButton (atom) | `packages/ui/src/AddSectionButton.jsx` | — | 2026-05-19 | Prominent "add new section" affordance shown once at the very bottom of all sections in Home edit mode (secondary entry point; the primary is the top-bar "Add Section" Button via `<Button variant="secondary" size="lg">`). Anatomy: outer `.add-section-button` (position relative, w 100%, h 48, transparent fill, 1px CB/600 top border) + inner `.add-section-button__pill` (position absolute, top -18 so the pill's vertical center sits ON the border line, 36×36, radius-full, CB/600 bg, white color cascading to the lucide/plus icon via currentColor). Polymorphic pill — `<button>` when `onClick` is provided, decorative `<span>` otherwise. Interactive state ladder (only on `.add-section-button__pill--interactive`): idle CB/600 → hover CB/400 (lightens) → active DSN/900 (darkens — mirrors ButtonLink). Transition: `background var(--transition-fast)`. Line itself is purely decorative — only the pill is interactive. Props: `onClick` (optional — drives polymorphic switch + `--interactive` modifier), `ariaLabel` (string, default `'Add section'`), `className`. Figma component `2210:302` on Components-Atoms (Panels artboard). Single-state master; hover/active are CSS-only. Code Connect: `packages/ui/src/AddSectionButton.figma.tsx`. **Used by:** none yet — will be consumed by the upcoming Home sections refactor as the final affordance at the end of the sections list. |
| AddSectionDivider (atom) | `packages/ui/src/AddSectionDivider.jsx` | `text-label-sm-medium` (label) | 2026-05-19 (refined same day — non-interactive) | **Purely decorative** row separator that announces the start of the to-be-added section above the AddSectionButton in Home edit mode. Renders as `<div role="separator" aria-label={label}>`. NOT a button — no `onClick`, no polymorphism, no hover/active states. The clickable affordance is the AddSectionButton that follows it. Visual: full-width × min-height 32, padding 6 14 raw (strategic-tokens exception), transparent fill, `border: 0` + `border-top: 1px dashed var(--deep-sea-neutral-400)` (no other borders), centered label `text-label-sm-medium` color DSN/400. Replaces a legacy external-library button instance — rebuilt with our primitives, all colors bound. Props: `label` (string, default `'Add Section'`), `className`. Figma component `2203:297` on Components-Atoms (Panels artboard). Single `Label` TEXT property. Code Connect: `packages/ui/src/AddSectionDivider.figma.tsx`. **Used by:** Home edit mode (single instance above AddSectionButton). *Initial release on 2026-05-19 included a polymorphic interactive variant with a hover/active ladder; same-day refinement removed those states per user direction — divider is a label, not an action.* |
| SectionLabel (atom) | `packages/ui/src/SectionLabel.jsx` | `text-label-sm-medium` (label) | 2026-05-19 | Section header row for widget rows on Home. 2 `Mode` variants — `default` (transparent surface, label only) and `edit` (DSN/100 surface, label + pencil + trash actions). Width FILL parent, min-height 36, padding 6/14 raw (strategic-tokens exception), radius-lg, primaryAxis space-between, counterAxis center. Action buttons are bare `<button>`s composing the new shared **`.icon-action` utility** (cursor + transition + hover `--text-primary` + active `--text-placeholder`) plus a slim `.section-label__action` component class (layout reset + idle `--text-tertiary`). Actions only present in `edit` mode (Default variant has no Section Actions frame at all). Real `lucide/pencil` + `lucide/trash-2` from Icons lg (static slots — per-instance stroke override to DSN/500 on the Figma master). Props: `label` (string, required), `mode` (`'default'` \| `'edit'`, default `'default'`), `onEdit` (optional), `onDelete` (optional). Figma component set `2198:308` on Components-Atoms (Panels artboard) with `Mode=Default` `2198:299` + `Mode=Edit` `2198:301`. Single `Label` TEXT property. Code Connect: `packages/ui/src/SectionLabel.figma.tsx` — maps Mode enum + Label TEXT. **Used by:** none yet — will be consumed by the upcoming Home sections refactor. |
| EntityChip (molecule) | `packages/ui/src/EntityChip.jsx` | — | 2026-05-07 (extended 2026-05-18) | Pill that names a scope (e.g. "Customers") and shows entity count via stacked dashed-border icon slots. **Display rules:** count 1-3 → that many handshake icons; count 4+ → 3 handshakes + "+N" slot where N = count − 3, **capped at +9**. Minimum 1 enforced via `Math.max(1, count)`. Default `entityIcon` is Handshake — consumers pass a different lucide icon for non-customer scopes. Pill: white bg + DSN/300 1px border + `--radius-full` + padding `6 var(--spacing-2) 6 var(--spacing-3)` (top/bottom 6 raw, off-scale carry-forward). Each slot: 24×24, 2px dashed DSN/300 border, `--spacing-1` padding, `--radius-full`, white bg. Slots overlap 4px via `margin-left: -4` on stacked slots (CSS `gap` doesn't accept negatives). "+N" text uses xs/xs Inter Semi Bold DSN/700 with `font-variant-numeric: tabular-nums`. Trailing add button is an IconButton instance configured with `<Plus size=16>` (overlaps -4 with previous slot). Props: `name` (default "Customers"), `count` (default 1), `entityIcon` (default `Handshake`), `showAddButton` (default true), `onAddClick`, `onClick`, `className`, `...rest`. Figma component `1716:60`: `Entity name` TEXT, `Show handshake 2` BOOLEAN (default true), `Show handshake 3` BOOLEAN (default true), `Show count` BOOLEAN (default true), `Count` TEXT (default "+1"), `Show add button` BOOLEAN (default true). The IconButton instance inside has `isExposedInstance: false` so designers can't swap its icon at the EntityChip level. **Home domain — not used in Shipments.** Not yet wired into a consumer. Code Connect: `packages/ui/src/EntityChip.figma.tsx`. *Updated 2026-05-07 (Session 21):* `onClick` prop dropped — chip is now decorative. **`onAddClick` re-added** (forwards to the inner IconButton's `onClick`). Polymorphic shell removed — always renders as `<div>`. `entity-chip--interactive` CSS removed. Vertical padding **4 (was 6) — code-only divergence**, Figma master keeps 6. Slot dashed border replaced by inline SVG ring (`stroke-dasharray="3 2"`, ~16 dashes; positioned `inset:-2px` to extend OUTSIDE the slot, consuming part of pill padding). Idle `--shadow-sm` kept on the pill, no state shift. Color cascades via `color: inherit` on slot, overflow, and inner IconButton (icons use `currentColor`) — but the chip's color stays static at `--text-secondary`. **Figma variant set deliberately not pushed** (chip stays a single static master). |
| FormField (molecule) | `packages/ui/src/FormField.jsx` | — | 2026-05-20 | Label + input pair. Switchable trailing-icon slot (INSTANCE_SWAP in Figma + `trailingIcon` ReactNode prop in code). States: Default, Focus (via `:focus-within`), Error (`error` prop). Error border 2px (manual Figma update mirrored in code). Auto-defaults `trailingIcon` to `<CircleAlert />` when `error` truthy and no explicit slot. Figma master `2255:98` on Components-Molecules. |
| AuthModal (organism) | `packages/ui/src/AuthModal.jsx` | — | 2026-05-20 | Pre-auth screen shell. OdysseyLogo (dark variant) baked into the header; consumer composes content via children. Used by Login route at `/` only. Backdrop is DSN/500 placeholder for now; bg.webp integration is the next iteration. Figma master `2244:1373` on Components-Organisms. *Note:* `Login.css` was deleted this cycle — consumer-level login styles now live as `.auth-content__*` in `components.css` (absorbed into AuthContent). |
| AuthContent (organism) | `packages/ui/src/AuthContent.jsx` | — | 2026-05-20 | Inner body of AuthModal shell. Variant axis currently single value `Login` (composes 2 × FormField + Log In Button + Forgot password link Button + account-link text). State (email + password) owned internally; callbacks: `onLogin({ email, password })`, `onForgotPassword`, `onCreateAccount`. Future variants (MfaSetup, PasswordSetup, etc.) extend the same master. Figma master `2264:712` on Components-Organisms. |

## Composite Text Utilities Created

| Utility Class | Properties | Used By | Safe to Purge? |
|---|---|---|---|
| `text-badge` | font-size: 12px; line-height: 16px; font-weight: 500 | Badge | No — Badge depends on it |

## Ad-hoc Implementations (NOT yet normalized)

These still use inline styles or local components. Do NOT purge their dependencies.

| Element | File | Blocks Purge Of | Notes |
|---|---|---|---|
| StatusBadge | `apps/odyssey-one/src/components/detail/RoutingGuideTab.jsx` | inline font-size: 12px, font-weight: 600 | May need new variant (semibold weight differs) |
| TypeBadge | `apps/odyssey-one/src/components/detail/DocumentsTab.jsx` | inline font-size: 12px, font-weight: 600 | Same pattern as StatusBadge |
| HazmatTag | `apps/odyssey-one/src/components/detail/ProductTab.jsx` | inline rgba colors, TriangleAlert 12px | Migrate to `Badge variant="amber" icon={...}` |
| Hazmat inline | `apps/odyssey-one/src/components/shipments/ShipmentTable.jsx` (grep TriangleAlert) | inline rgba colors, TriangleAlert 12px | Same as HazmatTag |
| Appointment badge | `apps/odyssey-one/src/components/detail/OrderTab.jsx` | inline rgba colors, font-size: 11px | Unique size — may need new text utility |
| History action badges | `apps/odyssey-one/src/components/detail/HistoryTab.jsx` | pill shape (border-radius: 9999px), font-size: 11px | Different shape — may need `pill` variant |
| Cost order tabs | `apps/odyssey-one/src/components/detail/CostAllocationTab.jsx` | inline rgba BADGE_BG/TEXT maps | Interactive tabs, not pure badges |
| Tab count pills | `apps/odyssey-one/src/components/shipments/ShipmentTabs.jsx` | font-weight: 700, border-radius: 10px | Count indicator, different pattern |

---

## Pending Figma Sync

Variants/props defined in code but not yet pushed to Figma. Push all at once when ready.

| Component | Variant/Prop | Description | Deferred On |
|---|---|---|---|
| Badge | pulse animation on `statusDot` | CSS keyframes — not representable in Figma. Documented in component description. | 2026-04-27 |
| Badge | asymmetric padding logic | `getPadding(icon, statusDot)` returns different padding per state. Figma uses static 2px 10px symmetric — code-only behavior. | 2026-04-27 |
| Badge | gray-only icon color override | Code: gray variant icon uses `--text-tertiary`; other variants use `currentColor`. In Figma, icon is an instance-swap slot — color comes from swapped icon content, not parent. Cannot model as a single variable binding. | 2026-04-27 |
| GlobalSearch | hover state — clear button (CircleX) | Default 400 → hover 200; Focused 200 → hover 100. Needs new Figma sub-variants or hover-flagged states. (Scope is now non-interactive — no scope hover needed.) | 2026-05-04 (scope-hover entry retired 2026-05-12) |
| WidgetsLeftMenu | group collapsed state | Code-only (chevron-down → chevron-right swap on click + items hidden via `collapsedGroups` Set). Figma master shows expanded state only. Future: add a `Collapsed` BOOLEAN component property or per-group state variants. | 2026-05-12 |
| SearchField | focus state | Code-only — input focus thickens border 1px → 2px (`--deep-sea-neutral-600`) and shifts search-icon color (added 2026-05-12, matches Shipments TableControls searchbar). Figma master shows idle state only. Future: add a `State` (Idle / Focused) variant set if designers need to spec focused mocks. | 2026-05-12 |
| IconButton | size 25×25 (vs 24×24 master) | Code: `.icon-button { width: 25px; height: 25px }` raised from 24px on 2026-05-13 (off-scale literal) — user requested mid-cycle, deferred from this normalize sync until they confirm functionality. Figma master stays 24×24 until user signs off. | 2026-05-13 |

## Pushed to Figma

Components pushed back to Figma via the `/normalize` skill's Step 8 routine.

| Component | Figma File | Component Set ID | Pushed | Notes |
|---|---|---|---|---|
| Badge | Design System - MCP (`vodiHJU38YWZYmTz81uOk7`) | `213:27` | 2026-04-27 | First push — seeded 5 variable collections (85 vars). Properties: Variant (6 colors), Show dot (bool), Show right icon (bool), Right icon (instance-swap), Show left icon (bool), Left icon (instance-swap). |
| Badge — leftIcon update | same | `213:27` | 2026-04-27 | Added Left icon slot + Show left icon + Left icon properties. Renamed Show icon → Show right icon, Icon → Right icon. Showcase has 4 columns: Default / Dot / Left / Right. **Convention: never set both leftIcon and rightIcon on a single Badge.** Code permits it structurally but it's not a valid state — the showcase intentionally omits a "Both" example. |
| Badge — metric variant | same | `213:27` (variant `1858:296`) | 2026-05-11 | Cloned from `gray` variant. Bg `Gray/bg`, text `Text/primary`, radius `Radius/lg`, padding `Spacing/2` horizontal + 1px vertical (off-scale raw), text style `label/xs semibold` (local). Dot + icon slot frames removed inside the variant only — component-set BOOLEANs (`Show dot` / `Show left icon` / `Show right icon`) stay but no-op for metric since the slot frames are absent. Used as the inline value pill inside WidgetMetricRow. |
| Widget set | same | `1825:7` (`Widget`) + `1825:8` (`WidgetContent`) | 2026-05-07 | 4 variants each (1x / 2x / 3x / 3xChart). Shell properties: `ContentSlot` SLOT, `Show grip` BOOLEAN, `Title` TEXT, `Domain icon` INSTANCE_SWAP. Content properties: `Value` TEXT, `Label` TEXT, `Percentage` TEXT. Go-to button inside 2x/3x/3xChart is a `ButtonLink` (`1838:7`) instance. 12 local text styles wired across all text nodes. |
| WidgetMetricRow | same | `1814:7` | 2026-05-07 | `Label` TEXT default "Date Issues", `Value` TEXT default "99", `Show indicator` BOOLEAN default false. Indicator dot fill defaults to `Chart/1`, consumer-overridable per instance. Trailing badge is a hand-built frame in this iteration; future cycle could replace with a `Badge variant=metric` instance now that the metric variant exists. |
| WidgetCtaRow | same | `1927:84` | 2026-05-12 | Components-Molecules page. Standalone master, single state (hover/pressed are code-only per WidgetMetricRow precedent). Properties: `Label` TEXT (default "Track a Shipment"), `Icon` INSTANCE_SWAP (default `placeholder-20`). Border-bottom bound to `Border/subtle`. Icon container bg bound to `Carolina Blue/50`. Radius-pill via variable. Used as 4 exposed instances inside WidgetContent `Variant=3xCta` (`1930:37`). |
| Widget set — `3xCta` variant + WidgetContent `3xCta` variant | same | Widget set `1825:7` (`Variant=3xCta` → `1934:715`); WidgetContent set `1825:8` (`Variant=3xCta` → `1930:37`) | 2026-05-12 | New variant added to both sets. Variant axis: `1x | 2x | 3x | 3xChart | 3xCta`. Widget 3xCta shell: padding 0, gap 0, radius `Radius/xl`, border `Border/subtle`, `shadow/sm`. Header sub-frame holds its own 24/24/24/12 padding + bottom border `Border/subtle`. WidgetContent 3xCta: VERTICAL stack of 4 `WidgetCtaRow` exposed instances; last instance has its `strokeBottomWeight` overridden to 0. Original standalone `CTA Widget` frame at `1916:337` renamed to `(deprecated — replaced by Widget set Variant=3xCta)` (kept, not deleted). |
| Carolina Blue/100 (NEW primitive) | same | variable `1921:7` | 2026-05-12 | New color primitive `#DCE8F7` — interpolated step between CB/50 (#F3F7FC) and CB/400 (#5BA4D4). Used as the hover/pressed bg for WidgetCtaRow's icon container. Also added to `tokens.css` as `--carolina-blue-100` and to `apps/odyssey-one/src/index.css` `@theme` as `--color-carolina-blue-100`. Scopes match CB/50 (ALL_SCOPES). |
| Widget shell border migration | same | all 4 existing Widget shell variants (`1774:1483`, `1774:1509`, `1774:1548`, `1774:1629`) | 2026-05-12 | Migrated stroke binding from `Deep Sea Neutral/300` (raw primitive) → `Border/subtle` (semantic alias to DSN/200). Mirrors code-side migration of `.widget` from `--border-default` to `--border-subtle`. Establishes the semantic rule: `--border-default` for input-like surfaces (badges/buttons/inputs); `--border-subtle` for card-like surfaces (widgets). |
| WidgetPieChart | same | `1881:77` (md `1880:78` / lg `1880:77`) | 2026-05-11 (md slimmed 2026-05-12) | Extracted from inline donuts inside WidgetContent2x + WidgetContent3xChart. `Size` VARIANT (md=72, lg=96), `Show center text` BOOLEAN (default false), `Center text` TEXT (default "42%"). **md = 1 segment (Chart/1) + Chart/rest ring** (2x single-data view). Segment 1 arcData represents a clean **42% arc from 12 o'clock going clockwise** (right side; `startingAngle ≈ -1.068 rad`, `endingAngle = π/2 rad`, sweep = 0.42·2π). **lg = 4 segments** (Chart/1 / Chart/2 / Chart/3 / Chart/4) + Chart/rest ring (3xChart multi-data view). Center text bound to `Text/secondary-soft` w/ local `label/sm medium` style. WidgetContent2x and WidgetContent3xChart reference instances of this master (`isExposedInstance: true`). |
| GlobalSearch — scope dropdown scrapped | same | set `658:18` (Default `639:563`, Focused `658:2`) | 2026-05-12 | Removed `Dropdown icon` INSTANCE_SWAP component property from the set. Deleted chevron-down instances inside both Search Scope frames (`471:2220` on Default, `658:11` on Focused). Rebalanced Search Scope padding L12 R8 (itemSpacing 12) → symmetric L12 R12 (itemSpacing 0) on both variants. Scope is now a single-child auto-layout frame containing just the "All" Caption. Title variant untouched. Mirrors code side — the scope chip is now a non-interactive label. |
| ButtonLink — State variants | same | set `1895:7` (Idle `1838:7` preserved, Hover `1894:7`, Pressed `1894:11`) | 2026-05-11 (padding 0 added 2026-05-12) | Converted single sm/Idle master into a 3-variant set. **Idle:** text + icon `Text/link` (CB/600). **Hover:** text + icon `Carolina Blue/400` primitive (#5BA4D4 — no `Text/*` semantic alias exists for CB/400 yet); auto-layout `itemSpacing` unbound, set to literal 20 (16 + 4, mirrors code's `translateX(4px)` on `.btn__icon--right`). **Pressed:** text + icon `Text/primary` (DSN/900); `itemSpacing` literal 20 (icon STAYS at +4 through press — matches code which has no `:active` reset). No Disabled variant in this cycle (no consumer needs it). Existing ButtonLink instance refs (`1850:77`, `:82`, `:86` in Widget 2x/3x/3xChart) still resolve via the preserved `1838:7` Idle variant inside the set. *Updated 2026-05-12:* **paddingLeft/Right/Top/Bottom = 0** on all 3 State variants — was 14/12/6/6. Width hugs content (Idle ~130, Hover/Pressed ~134 due to itemSpacing 20 vs 16). Matches code's `.btn--link { padding: 0; height: auto }` override of the size paddings. |
| SearchField | same | `1959:76` | 2026-05-12 | New molecule on Components-Molecules page. White bg, 1px `Border/default`, `Radius/md`, `shadow/sm`. Children: `lucide/search` (`1948:848` from Icons lg, user-added this session) + label/sm regular text. Component property: `Placeholder` TEXT default `"Search"`. Used as the search section inside WidgetsLeftMenu (FILL width). |
| WidgetsLeftMenu | same | `1961:393` | 2026-05-12 | New organism on Components-Organisms page. 240w. Composes our SearchField instance + `lucide/chevron-down` (`583:463`) + `lucide/chevron-right` (`1782:2303`) + `lucide/grip-vertical` (`1948:910`, user-added this session). Hover demo row on "Order Exceptions" with `DSN/100` bg + visible grip. Effect Style `shadow/panel`. **Built entirely from our library primitives** — zero references to external Untitled-UI-style kit assets (the original Figma source used kit icons + a Navbars/Search field instance; our master rebuilds from scratch using only our masters). |
| shadow/panel (NEW Effect Style) | same | Effect Style (no component-set id) | 2026-05-12 | 3-layer drop shadow for floating panels: ring + medium float + long float. Layers: `0 0 0 1px rgba(0,0,0,0.05)` (1px ring) + `0 4px 6px -2px rgba(0,0,0,0.05)` (medium) + `0 10px 15px -3px rgba(0,0,0,0.1)` (long). Mirrors the `--shadow-panel` token in `packages/tokens/tokens.css`. Used by WidgetsLeftMenu. |
| MenuRow | same | set `1973:87` (variants Default `1973:77` / Hover `1973:79` / Pressed `1973:83`) | 2026-05-12 | New molecule on Components-Molecules page. 3 State variants. `Label` TEXT property. Hover variant: bg `Deep Sea Neutral/100` + visible grip (opacity 1). Pressed variant: bg `Deep Sea Neutral/200` + visible grip. Uses `lucide/grip-vertical` (`1948:910`). Code-side states are CSS-driven (no React `state` prop) — the variants exist for designer-side spec parity. Used by MenuDropdown + WidgetsLeftMenu. |
| MenuDropdown | same | `1981:79` | 2026-05-12 | New molecule on Components-Molecules page. Properties: `Title` TEXT default `"Orders"`, `Expanded` BOOLEAN default `true`, `ContentSlot` SLOT (designer-only — plugin API can't override instance children on slots). Header binds `lucide/chevron-down` visibility to `Expanded`. Items frame binds visibility to `Expanded` + slot content to `ContentSlot`. |
| Widget 1x — add domain icon at lead + 22px gap + top-align content | same | set `1825:7`, variant `Variant=1x` (`1774:1483`) | 2026-05-12 | `primaryAxisAlignItems` SPACE_BETWEEN → MIN; `itemSpacing` 16 → 22 (matches code's 22px gap override on `.widget--1x`). New domain-icon instance inserted at lead in the Header title frame (between grip and title), then swapped to `placeholder-20` (`512:2395`) per icon-slot convention. Title container set to `layoutSizingHorizontal=FILL` with text-truncation `ENDING`. Content frame now top-aligned (matches code's removal of `.widget__content--1x` from the `margin-top: auto` list). |
| Widget 2x — domain icon to lead | same | set `1825:7`, variant `Variant=2x` (`1774:1509`) | 2026-05-12 | `clipboard-list` icon moved from the right-side "Button group" frame (next to X) to the "Header title" frame at lead (between grip and title text). The "Button group" frame now contains only the X icon. Code was already correct; this is a pure Figma drift-fix. |
| Widget set — all domain icons → `placeholder-20` | same | set `1825:7` | 2026-05-12 | 4 variants (1x / 2x / 3x / 3xChart) had their bound domain-icon instances swapped to `placeholder-20` (`512:2395` in Icons Placeholder): 1x `lucide/triangle-alert` → placeholder-20; 2x `lucide/clipboard-list` → placeholder-20; 3x `lucide/container` → placeholder-20; 3xChart `lucide/container` → placeholder-20. The set's `Domain icon#1828:5` INSTANCE_SWAP component-property default also updated from `lucide/container` (`583:415`) to `placeholder-20` (`512:2395`). 3xCta unaffected (no domain icon slot). Follows the icon-slot convention. |
| WidgetVariantPicker (moved to Modals artboard 2026-05-13) | same | set `2005:554` on Components-Organisms (Modals artboard `1997:434`) | 2026-05-12 (relocated 2026-05-13) | New 4-variant organism (originally classified as molecule, moved into the Modals artboard alongside ModalLarge / ModalMedium 2026-05-13 — reclassified as organism by virtue of its role and pairing with the modal shells) (`Variant=1x | 2x | 3x | 3xChart`). 496×460 each variant. Composes real `Widget` instances (no hand-built proxies), private radius-full `Picker arrow` frames (40×40 with lucide chevron-left/right lg inside), `Info container` (vertical fill), `Main container` (centers widget), `Dots container` (label `label/xs regular` + 4 ellipses). Active dot bound to `Deep Sea Neutral/900`; inactive dots `Deep Sea Neutral/300`. Picker bg bound to `Deep Sea Neutral/100`. Variants 3x and 3xChart have their nested Widget instance scaled to **0.85** via `rescale()` so the 360-wide widget visually breathes inside the modal. Nested Widget instances are `isExposedInstance: true` so designers can override `Title` / `Domain icon` / `Show grip`. |
| CustomerRow | same | set `2029:461` on Components-Molecules (variants `Favorite=False` `2028:448` / `Favorite=True` `2029:457`) | 2026-05-13 | New 2-variant molecule. 48h × 492w. Anatomy: Customer Info (icon container 24×24 with **padding 0** + 2px DSN/300 ring + radius-full, contains lucide/handshake md by default; INSTANCE_SWAP default = `placeholder-16` per icon-slot convention) + Label (`label/sm medium`, `Text/secondary`) + Customer Actions (lucide/star lg + lucide/trash-2 lg). Border-bottom `Border/subtle`. `Favorite=True` variant has the star's inner Vector fill overridden to `Text/secondary` (filled). Properties: `Label` TEXT (default "Customer 1"), `Icon` INSTANCE_SWAP (default placeholder-16), `Favorite` VARIANT. Default icon container padding = 0 (synced from code Step 9 — was 4px in original Figma master, dropped because the 16px icon looked cramped). |
| ModalMedium | same | `2032:915` on Components-Organisms (parented inside `Modals` artboard `1997:434`) | 2026-05-13 | New organism shell. 540w × autosize-height. Header (Title + Close container) / Content SLOT / Footer SLOT. Properties: `Title` TEXT (default "Modal title"), `Content` SLOT, `Footer` SLOT. Title bound to `heading/lg semibold` (NEW text style, see below). Modal fill = `White`, radius `Radius/xl`, effect `shadow/2xl` (NEW Effect Style, see below). Header padding `Spacing/6 Spacing/6 Spacing/3 Spacing/6`; Content padding `Spacing/3 Spacing/6 0 Spacing/6` (bottom 0 so list sits flush to the footer divider); Footer padding `Spacing/3 Spacing/6` with `Border/subtle` top border. Close container `Radius/lg` (rounded-square hover area; was `Radius/full` in the original GATE-A build, updated 2026-05-13 to match code Step 9). Default Content SLOT = labeled SearchField (`Show label=true, Show info icon=true, Label="Add Customers", Placeholder="Search Customers"`) + 3 CustomerRow instances (first `Favorite=True`). Default Footer SLOT = Cancel (Button Secondary lg) + Save (Button Primary lg). **Nested default-content instances are NOT exposed** (`isExposedInstance=false` on SearchField, all 3 CustomerRows, both buttons) — designers select each sub-instance directly. A demo instance `ModalMedium — Add Customers` (`2033:963`) sits in the Modals artboard with title overridden to "Add Customers" for design-time reference. The user's earlier hand-built `ModalMedium` frame at `2025:2137` was superseded — present elsewhere on the Components-Organisms page until user removes it. |
| heading/lg semibold (NEW text style) | same | local text style (no component-set id) | 2026-05-13 | New text style: 18px / 24px line-height / 600 weight Inter, bound to `Font/primary`, `Font Size/lg`, `Line Height/lg`, `Font Weight/semibold`. Used by ModalMedium title. Matching `.text-heading-lg-semibold` utility class added to `apps/odyssey-one/src/styles/components.css`. |
| shadow/2xl (NEW Effect Style) | same | Effect Style (no component-set id) | 2026-05-13 | Deeper drop shadow for elevated modals: `0 25 50 -12 rgba(0,0,0,0.25)`. Single-layer DROP_SHADOW. Mirrors the `--shadow-2xl` token added to `packages/tokens/tokens.css`. Used by ModalMedium. |
| SearchField — label + info icon extension | same | `1959:76` | 2026-05-13 | Master restructured to a VERTICAL auto-layout: optional Label row (lucide/info md + label text bound to `label/sm medium`, color `Text/secondary` / `Text/tertiary`) above the existing input bar (wrapped into a new `Input bar` sub-frame). 3 new properties: `Show label` BOOLEAN (default false), `Label` TEXT (default "Label"), `Show info icon` BOOLEAN (default false). Existing `Placeholder` TEXT property unchanged. Defaults keep the field flat — WidgetsLeftMenu's existing usage is unaffected. ModalMedium consumer overrides to `Show label=true, Show info icon=true, Label="Add Customers", Placeholder="Search Customers"`. The Placeholder text node's `componentPropertyReferences.characters` had to be re-bound after the appendChild reparent (binding was lost during restructure). |
| EntityChip name → label/sm medium | same | `1716:60` (text node `1694:549`) | 2026-05-13 | Renamed the chip's "Customers" / "Add Customers" name text from `font-weight-regular` (400) to `font-weight-medium` (500) via the `label/sm medium` text style. Matches the code-side `.entity-chip__name` font-weight change. Visible on both non-edit ("Customers") and edit-mode ("Add Customers") chip labels. |
| Close button radius — ModalLarge | same | close container `2006:668` | 2026-05-13 | Radius binding changed from `Radius/full` (pill) → `Radius/lg` (8px rounded-square). Mirrors the code-side `.modal-large__close { border-radius: var(--radius-lg) }` update. The code also applies `margin-right: calc(-1 * --spacing-2)` for optical alignment with the modal's right edge — code-only (Figma masters don't model the offset). |
| Close button container — ModalMedium | same | new Close frame `2046:1054` wrapping the existing lucide/x | 2026-05-13 | Wrapped the master's bare lucide/x in a 40×40 `Close` container with `Radius/lg` corners + 10px padding. Brings ModalMedium's close-button structure to parity with ModalLarge's pattern (so designers can see the hover-area shape) and reflects the code-side rounded-square hover treatment. `margin-right: calc(-1 * --spacing-2)` is code-only. |
| ModalLarge | same | `2006:663` on Components-Organisms (parented inside `Modals` artboard `1997:434`) | 2026-05-12 | New organism shell. 496-wide, header (12/12/8/16 padding — bottom 8 = `Spacing/2`, gap 0 per spec round 2) / Content SLOT (default = WidgetVariantPicker 1x instance) / Footer SLOT (default = Button Primary lg "Insert widget" full-width). Header bottom border bound to `Border/subtle`. Modal radius `Radius/xl`, fill `White`, shadow `shadow/md`. Properties: `Title` TEXT (default "Widget name"), `Subtitle` TEXT (default "Domain"), `Show subtitle` BOOLEAN (default true, bound to subtitle text visibility), `Content` SLOT, `Footer` SLOT. The `Modals` artboard contains the master + 4 ready-made instances (`ModalLarge — 1x / 2x / 3x / 3xChart`) each preset to a different picker variant for design-time reference. **The earlier hand-built `Metric view` component_set placeholder at `1997:1201` was deleted as part of this push** (replaced by the proper master + variants). |
| label/sm semibold (NEW text style) | same | local text style (no component-set id) | 2026-05-12 | New text style: 14px / 20px line-height / 600 weight Inter, bound to `Font/primary`, `Font Size/sm`, `Line Height/sm`, `Font Weight/semibold`. Mirrors naming pattern `label/xs semibold` (existing). Used by ModalLarge title. Matching `.text-label-sm-semibold` utility class added to `apps/odyssey-one/src/styles/components.css`. |
| Badge — favorite variant | same | set `213:27` (variant `2096:294`) | 2026-05-18 | 9th variant `Variant=favorite` added. 12×12 Caribbean Green/600 disc, padding 2, Radius/full, contains a centered 8×8 lucide/star (`2025:2350` from Icons lg, scaled to 8) with Vector fill+stroke overridden to `White`. Cloned from `Variant=notification` (`1256:2`) — inherited child layers (Dot / Left icon / "6" / Right icon) all removed; pure single-star content. Used as the favorite-overlay atom inside CustomerRow `Mode=List, Favorite=True` variants. Original standalone BookmarkedBadge atom at `2089:293` was deleted (orphan removed). |
| CustomerRow — Mode axis (Result variant added) + logo 32 + Badge favorite overlay | same | set `2029:461` | 2026-05-18 | Added second variant axis `Mode=List | Result` — now 4 variants total (Mode=List/Favorite=False, Mode=List/Favorite=True, Mode=Result/Favorite=False, Mode=Result/Favorite=True). **List mode** (existing): 48h row, **logo container 32×32** (was 16×16) with Radius/full + 2px DSN/300 inside-stroke + padding 6, inner placeholder swapped 16 → 20 (`512:2395` placeholder-20); **Star action removed entirely** (only Trash remains as action); `Favorite=True` adds a `Badge[Variant=favorite]` instance at logo bottom-right (absolute position `22,22`). **Result mode** (new): 40h row, padding `8/16/8/16`, logo container 24×24 with placeholder-16 inside, action = `lucide/star` 16px (from Icons md `2091:1844`) — `Favorite=False` outline (Vector stroke DSN/400, fill empty), `Favorite=True` filled (Vector fill+stroke DSN/900). INSTANCE_SWAP `Icon` default updated from placeholder-16 to placeholder-20 (auto-updated via swapComponent on the List variants). |
| SearchField — Results slot | same | `1959:76` | 2026-05-18 | Added a new `Results slot` frame child (`2093:100`) after the Input bar, visibility bound to a new `Show results` BOOLEAN property (default false). Frame is HUG-vertical / FILL-horizontal, empty by default — consumers populate it with dropdown rows (e.g. CustomerRow Result-mode instances) via the React `results` prop. |
| Icons md — lucide/star (NEW master) | same | `2091:1844` | 2026-05-18 | User-added via Lucide plugin. 16px master, key `1f5c30f8252413af803d8df3aa6b5cbc6adc5f64`. Needed for the CustomerRow Result-mode star toggle (16×16, distinct from the existing 20px star at `2025:2350` in Icons lg). |
| IconButtonGhost (NEW component set) | same | set `2138:304` on Components-Atoms (Small Buttons artboard) | 2026-05-18 | 28×28 ghost icon button. 3 State variants (Idle `2138:298` / Hover `2138:300` / Pressed `2138:302`). Backgrounds: Idle transparent, Hover DSN/100, Pressed DSN/200. Radius/lg (8) bound on all 4 corners. INSTANCE_SWAP `Icon` property defaulting to `placeholder-20`. After creation, set was moved into the existing Small Buttons artboard alongside the circular shadowed IconButton (parent-pair convention). |
| Modal/Widget Close swap → IconButtonGhost instance | same | 3 masters: ModalMedium `2032:915` (new Close inst `2143:910`), ModalLarge `2006:663` (`2143:912`), Widget set `1825:7` — all 5 variants (1x `2143:914`, 2x `2152:908`, 3x `2152:912`, 3xChart `2152:916`, 3xCta `2152:920`) | 2026-05-18 | Replaced each master's inline close button (40×40 frame with bare lucide/x, or in Widget's case various structures per variant — "Button group" wrapping x in 2x, "Close container" in 3x/3xChart, bare x in 3xCta) with an `IconButtonGhost` instance overridden to `Icon=lucide/x` (`1570:2`). Uniform 28×28 close button across the entire system now. Variant=2x had a wrapping "Button group" frame that was removed entirely in favor of the direct IconButtonGhost instance. |
| EmptyState (NEW component) | same | `2159:295` on Components-Atoms (**new Panels artboard `2159:294`**) | 2026-05-18 | New atom. Vertical auto-layout, padding 24 (`Spacing/6`), gap 8 (`Spacing/2`), centered. Background bound to `Deep Sea Neutral/100`, all 4 corner radii bound to `Radius/lg`. Children: a 32×32 placeholder-20 instance (icon slot, INSTANCE_SWAP) + a Message text bound to local `label/xs regular` style. Properties: `Icon` INSTANCE_SWAP (default placeholder-20) + `Message` TEXT (default "No items to show yet."). Used in code by Home's Add Customers modal empty state. Also created the parent `Panels` artboard (`2159:294`) — new top-level artboard on Components-Atoms, ready to hold future panel atoms. |
| ModalMedium — header padding update | same | `2032:915` header frame | 2026-05-18 | Header padding rebalanced 20/24/16/24 (user-side update) and subsequently 20/24/20/24 (code-side adjustment) to compensate for the smaller 28×28 close button replacing the 40×40 frame. Symmetric vertical padding. |
| Add Customers staging — token drift cleanup | same | parent staging `2059:988` (4 Add Customers frames) | 2026-05-18 | 39 visible text nodes rebound from legacy library text styles (`inter-text-sm/leading-5/font-medium`, `text-base/leading-6/font-medium`, etc.) → local `label/* medium` and `heading/lg semibold`. 152 visible solid fills rebound to local DSN + White + Caribbean Green/600 variables. Hidden subtrees (unused legacy layout paths) skipped intentionally — they'll be removed when staging is rebuilt with the new component instances. Eliminates the external library dependency that the publish would have dragged through. |
| Button — Disabled state re-spec (all 4 variants × 3 sizes) | same | set `1307:333`, 12 variants (Primary/Secondary/Outline/Ghost × sm/md/lg, all `State=Disabled`) | 2026-05-19 | Visual re-spec of the existing Disabled axis: from "filled-pill" (`DSN/300` bg, white text, no border) → **"muted bordered" (White bg, `DSN/300` border 1px inside, `DSN/300` label, `DSN/300` icon placeholder stroke)**. All 4 fills/strokes/text fills bound to existing variables (`Deep Sea Neutral/300` `212:6` + `White` `212:13`). Icon `placeholder-20` inside each Disabled variant got per-instance stroke override → DSN/300 (was placeholder's default purple). Geometry untouched (padding/gap/radius preserved). **No new tokens, icons, or text styles added** — all reuses existing primitives. Spec frame at `2167:4698` (Components-Atoms, standalone reference) was the source of truth — also cleaned of drift in the same cycle (raw hex → bound variables, external library text style → local `label/sm medium`, legacy `icons/20px/download` → `placeholder-20`). Sibling spec frames `2169:777` (IconButton-shaped, 32×32 r=6) + `2169:775` (legacy library button reference) intentionally left untouched — IconButton + IconButtonGhost disabled is a separate cycle. |
| Dead Button-disabled tokens removed | repo `packages/tokens/tokens.css` | n/a (token cleanup) | 2026-05-19 | Removed unused `--btn-disabled-bg` (`DSN/300`) + `--btn-disabled-text` (`text-inverse`) component tokens — represented the old "filled-pill" disabled approach. Grep across `apps/` + `packages/` confirmed zero `var(--btn-disabled-*)` consumers before removal. Also removed the 2 stale swatches in DesignSystemMap Foundations Colors tab that displayed them. |
| SectionLabel (NEW component set) | same | `2198:308` on Components-Atoms (Panels artboard) | 2026-05-19 | New atom. 2 `Mode` VARIANT axis — `Default` `2198:299` (transparent fill, text only) + `Edit` `2198:301` (DSN/100 bound fill, text + Section Actions: lucide/pencil + lucide/trash-2 lg, gap 12, per-instance stroke override → DSN/500). Both: 400w (default) × 36h, padding 6/14, radius-lg bound on all 4 corners, primaryAxis SPACE_BETWEEN, counterAxis CENTER. Single `Label` TEXT property default `"Section name"` wired via `componentPropertyReferences.characters` on both variants. Used by the upcoming Home sections refactor. **All values bound to existing variables** — no new tokens/icons/text styles added. |
| AddSectionButton (NEW component) | same | `2210:302` on Components-Atoms (Panels artboard) | 2026-05-19 | New atom. Single state (no variant axis — hover/active are CSS-only via `.add-section-button__pill--interactive:hover/:active`). Parent FRAME 400w × 48h, transparent fill, strokeTop 1px Carolina Blue/600 bound + others 0, primaryAxis CENTER, counterAxis CENTER, padding 0. Pill child FRAME 36×36 with `layoutPositioning='ABSOLUTE'`, positioned at x=(400-36)/2 (centered horizontally) + y=-18 (so pill's vertical center sits ON the parent's top edge, straddling the border line). Pill fill bound to CB/600, all 4 corner radii bound to `Radius/full`, padding 8, VERTICAL auto-layout centered. Pill contains a `lucide/plus` instance (20px from Icons lg `1303:5`) with per-instance Vector strokes overridden to `White` bound. **Replaces** the hand-built "Secondary Add Section Button" frame in the Home Edit Mode mockup — rebuilt with our primitives, all colors bound, no external library refs. |
| AddSectionDivider (NEW component) | same | `2203:297` on Components-Atoms (Panels artboard) | 2026-05-19 | New atom. Single state (no variant axis — hover/active are CSS-only via `.add-section-divider--interactive:hover/:active`). 400w (default) × 32h, padding 6/14, transparent fill, strokeTop 1px DSN/400 bound + strokeRight/Bottom/Left = 0, dash pattern `[2, 2]`. Single TEXT child "Add Section" centered (primaryAxis CENTER, counterAxis CENTER), Inter Medium 14/20 = local `label/sm medium`, fill DSN/400 bound. `Label` TEXT property default `"Add Section"` wired via `componentPropertyReferences.characters`. **Replaces** the previous legacy library instance used as the inline "Add Section" divider in the Home edit-mode mockup. All colors + text style bound to our system — no external library dependencies. |
| .icon-action shared utility (NEW) | repo `apps/odyssey-one/src/styles/components.css` | n/a (CSS utility extraction) | 2026-05-19 | New shared interactive-affordance utility: `.icon-action { cursor + transition + :hover --text-primary + :active --text-placeholder }`. Owns the cross-cutting "bare clickable icon" color ladder previously duplicated across `.widget__grip` (edit subset), `.customer-row__action`, `.menu-dropdown__header`, and the brand-new `.section-label__action`. Idle color stays per-component (varies — SectionLabel uses `--text-tertiary`, CustomerRow uses `--text-secondary`). Consumers compose: `<button className="icon-action component-class">`. **Migrated this cycle:** SectionLabel (greenfield). **Pending migration** (tracked separately): customer-row__action, widget__grip edit subset, menu-dropdown__header. |
| FormField (NEW component set) | same | `2255:98` on Components-Molecules | 2026-05-20 | New molecule. 3 State variants (Default / Focus / Error). Properties: `Label` TEXT, `Placeholder` TEXT, `Show icon` BOOLEAN (default false), `Icon` INSTANCE_SWAP (default `placeholder-16` at `213:2`). Error state border 2px (user adjusted manually mid-cycle, mirrored in code). All values bound to existing variables — no new tokens/icons/text styles added. |
| AuthModal (NEW component) | same | `2244:1373` on Components-Organisms / Modals | 2026-05-20 | New organism shell. Single-state master. OdysseyLogo dark variant instance (`484:2266`) inside a centered HEADER frame, content slot below. Consumer composes children for the body. Backdrop placeholder bound to DSN/500 (bg.webp integration deferred to next iteration). |
| AuthContent (NEW component) | same | `2264:712` on Components-Organisms / Modals | 2026-05-20 | New organism. Composes existing FormField (`2255:98`) × 2 + Button Primary lg (`1307:333`) + Button Link (`1895:7`) instances. Spacing: outer gap 32 (`var(--spacing-8)`), inner form gap 16 (`var(--spacing-4)`). Account-link text has an underlined inline span for the "Create an account." portion. All values bound to existing variables — no new tokens/icons/text styles added. |

## Pushed to Figma → Code Connect

Mappings published via `npx figma connect publish` from `packages/ui/`. Re-run when a `.figma.tsx` is added or modified.

| Component | Mapping File | Figma Node | Published | Notes |
|---|---|---|---|---|
| Badge | `packages/ui/src/Badge.figma.tsx` | `213:27` | 2026-04-28 | First mapping. Explicit `imports: ["import { Badge } from '@odyssey/ui'"]` to override auto-detected relative path. |
| SidebarButton | `packages/ui/src/SidebarButton.figma.tsx` | `514:2479` | 2026-05-04 | Maps `Property 1` enum → `state` ('default'/'hover'/'selected'); `Icon` instance → `icon` slot. |
| GlobalSearch | `packages/ui/src/GlobalSearch.figma.tsx` | `658:18` | 2026-05-04 (dropdown removed 2026-05-12) | 3 `figma.connect()` blocks per State variant. Default + Focused → `<GlobalSearch mode="search" />` (no props — focus is internal); Title → `<GlobalSearch mode="title" title={title} />`. `Dropdown icon` mapping removed after the scope dropdown was scrapped. |
| LeadNav | `packages/ui/src/LeadNav.figma.tsx` | `639:564` | 2026-05-04 | Maps `Logo` INSTANCE_SWAP → `logo` slot. The logo's nested variant property (`Property 1`: Light/Dark) is exposed via `isExposedInstance: true` on the inner logo instance, so LeadNav instances expose both the swap picker AND the variant dropdown. |
| TrailNav | `packages/ui/src/TrailNav.figma.tsx` | `639:562` | 2026-05-04 | Maps Name/Role TEXT → `name`/`role`, Show notification BOOLEAN → `showNotification`, Chevron INSTANCE_SWAP → `chevron` slot. Avatar consumer-provided. (Renamed from `User` 2026-05-04.) |
| Button | `packages/ui/src/Button.figma.tsx` | `1307:333` | 2026-05-05 | Maps `Variant` enum → `variant` (Primary/Secondary/Outline → primary/secondary/outline), `Size` enum → `size` (sm/md/lg), `State` enum → `disabled` (Idle/Hover/Pressed → false, Disabled → true; hover/pressed are CSS pseudo-classes at runtime). `Show icon` BOOLEAN + `Icon` INSTANCE_SWAP → `icon` prop (gated by figma.boolean true/false). `Label` TEXT → children. |
| IconButton | `packages/ui/src/IconButton.figma.tsx` | `1754:295` (set; variants `1711:297` / `1753:295` / `1753:297`) | 2026-05-07 | Maps `Icon` INSTANCE_SWAP → `icon` prop. State variants all resolve to the same React component — state is CSS-driven, no React `state` prop. |
| PageHeader | `packages/ui/src/PageHeader.figma.tsx` | `1693:49` | 2026-05-07 | Maps `Title` TEXT → `title` prop. |
| SectionHeader | `packages/ui/src/SectionHeader.figma.tsx` | `1696:49` | 2026-05-07 | Maps `Title` TEXT → `title`, `Supporting text` TEXT → `supportingText`. |
| EntityChip | `packages/ui/src/EntityChip.figma.tsx` | `1716:60` | 2026-05-07 | Maps `Entity name` TEXT → `name`, `Show add button` BOOLEAN → `showAddButton`. Example uses `count={4}` to demonstrate the +N overflow visually. |
| Widget | `packages/ui/src/Widget.figma.tsx` | `1825:7` | 2026-05-07 | Maps `Variant` enum → `variant` (`1x`/`2x`/`3x`/`3xChart`), `Title` TEXT → `title`, `Show grip` BOOLEAN → `showGrip`, `Domain icon` INSTANCE_SWAP → `domainIcon`. |
| WidgetMetricRow | `packages/ui/src/WidgetMetricRow.figma.tsx` | `1814:7` | 2026-05-07 | Maps `Label` / `Value` TEXT → `label` / `value`, `Show indicator` BOOLEAN → `showIndicator`. Indicator color not exposed in Figma — consumers override the dot fill per-instance to a `Chart/*` token. |
| Badge — metric variant | `packages/ui/src/Badge.figma.tsx` | `213:27` | 2026-05-11 | Existing mapping extended — `metric` added to the `variant` enum picker. Iconless variant; `Show left icon` / `Show right icon` BOOLEANs no-op since the slot frames are absent. |
| WidgetPieChart | `packages/ui/src/WidgetPieChart.figma.tsx` | `1881:77` | 2026-05-11 | Maps `Size` enum → `size` (`md`/`lg`), `Show center text` BOOLEAN + `Center text` TEXT → `centerText` (gated; when false → undefined). Segments + colors are data-driven in code — example renders 4 chart-token-bound segments. |
| ButtonLink — State set | `packages/ui/src/Button.figma.tsx` (second `figma.connect`) | `1895:7` (was `1838:7`) | 2026-05-11 | Mapping URL updated from the single-variant master `1838:7` to the new variant set `1895:7`. Other props unchanged (`Label` TEXT → children, `Show icon` BOOLEAN + `Icon` INSTANCE_SWAP → `iconRight`). State variants resolve to the same React snippet — `variant="link"` covers all 3 states via CSS pseudo-classes. |
| SearchField | `packages/ui/src/SearchField.figma.tsx` | `1959:76` | 2026-05-12 | Maps `Placeholder` TEXT → `placeholder` prop. Value/onChange/onClear are runtime — not surfaced in Figma. |
| WidgetsLeftMenu | `packages/ui/src/WidgetsLeftMenu.figma.tsx` | `1961:393` | 2026-05-12 | Maps `Title` TEXT → `title` prop. **Library-pure** — no DnD coupling; the `renderItem(item, group, defaultNode)` render-prop is consumer-provided for DnD adapters (e.g. wrapping each row in `useSortable` from @dnd-kit on the Home edit-mode consumer). |
| MenuRow | `packages/ui/src/MenuRow.figma.tsx` | `1973:87` | 2026-05-12 | 3 `figma.connect()` blocks — one per State variant (Default `1973:77` / Hover `1973:79` / Pressed `1973:83`). All three map `Label` TEXT → `label` and render the identical snippet (state is CSS-only in code; React component does not expose a `state` prop). |
| MenuDropdown | `packages/ui/src/MenuDropdown.figma.tsx` | `1981:79` | 2026-05-12 | Single mapping. `Title` TEXT → `title`; `Expanded` BOOLEAN → `expanded`. SLOT (`ContentSlot`) not mapped (mirrors Widget's pattern — slot children live outside Figma component props; designer-managed only). |
| ModalLarge | `packages/ui/src/ModalLarge.figma.tsx` | `2006:663` | 2026-05-12 | Single mapping. `Title` TEXT → `title`, `Subtitle` TEXT → `subtitle`, `Show subtitle` BOOLEAN → `showSubtitle`, `Content` SLOT → `children`, `Footer` SLOT → `footer`. `onClose` is consumer-supplied at runtime, not exposed in Figma. |
| WidgetVariantPicker | `packages/ui/src/WidgetVariantPicker.figma.tsx` | `2005:554` | 2026-05-12 | 4 `figma.connect()` blocks — one per Variant (1x / 2x / 3x / 3xChart). All four render the identical JSX (`<WidgetVariantPicker variant={V} onVariantChange={() => {}} />`) with the matching `variant` prop. Interactive behavior (arrows, dot clicks, infinite wrap) is code-only. |
| ModalMedium | `packages/ui/src/ModalMedium.figma.tsx` | `2032:915` | 2026-05-13 | Single mapping. `Title` TEXT → `title`, `Content` SLOT → `children`, `Footer` SLOT → `footer`. `onClose` is consumer-supplied at runtime. |
| CustomerRow | `packages/ui/src/CustomerRow.figma.tsx` | `2029:461` | 2026-05-13 | 2 `figma.connect()` blocks (one per Favorite variant, False / True). Both map `Label` TEXT → `label` and `Icon` INSTANCE_SWAP → `icon`. The Favorite variant maps to the React `favorite` boolean prop, which drives the Star's `fill="currentColor"` at runtime. |
| SearchField — extended with label + info | `packages/ui/src/SearchField.figma.tsx` | `1959:76` | 2026-05-13 | Existing mapping extended — new prop mappings: `Show label` BOOLEAN → `showLabel`, `Label` TEXT → `label`, `Show info icon` BOOLEAN → `showInfoIcon`. Existing `Placeholder` TEXT → `placeholder` unchanged. |
| Badge — favorite variant | `packages/ui/src/Badge.figma.tsx` | `213:27` | 2026-05-18 | Generic mapping extended (`favorite` added to enum); plus a dedicated `variant: { Variant: 'favorite' }` block rendering the simpler snippet `<Badge variant="favorite" />` (no children, no slots) — favorite is icon-only and decorative, so the dedicated block produces cleaner Dev Mode output for that specific variant. |
| CustomerRow — Mode + Favorite axes | `packages/ui/src/CustomerRow.figma.tsx` | `2029:461` | 2026-05-18 | Replaced the previous 2 variant-specific mappings with a single `figma.connect` using two enums: `Mode` (`List` → `'list'`, `Result` → `'result'`) and `Favorite` (`True` → `true`, `False` → `false`). Plus `Label` TEXT → `label` and `Icon` INSTANCE_SWAP → `icon`. Example renders `<CustomerRow mode={mode} favorite={favorite} label={label} icon={icon} />`. |
| IconButtonGhost | `packages/ui/src/IconButtonGhost.figma.tsx` | `2138:304` | 2026-05-18 | Single mapping. `Icon` INSTANCE_SWAP → `icon` prop. State variants resolve to the same React snippet (state is CSS-driven). Example uses `onClick={() => {}}` placeholder so Dev Mode shows the polymorphic `<button>` form. |
| EmptyState | `packages/ui/src/EmptyState.figma.tsx` | `2159:295` | 2026-05-18 | Single mapping. `Icon` INSTANCE_SWAP → `icon`, `Message` TEXT → `message`. Renders `<EmptyState icon={icon} message={message} />`. |
| SectionLabel | `packages/ui/src/SectionLabel.figma.tsx` | `2198:308` | 2026-05-19 | Single mapping. `Mode` enum → `mode` (`Default` → `'default'`, `Edit` → `'edit'`); `Label` TEXT → `label` prop. Example uses `onEdit={() => {}}` + `onDelete={() => {}}` placeholders so Dev Mode shows the rendered `<button>`s in edit mode. |
| AddSectionDivider | `packages/ui/src/AddSectionDivider.figma.tsx` | `2203:297` | 2026-05-19 | Single mapping. `Label` TEXT → `label` prop. Example uses `onClick={() => {}}` placeholder so Dev Mode shows the polymorphic `<button>` form with the `--interactive` modifier active. |
| AddSectionButton | `packages/ui/src/AddSectionButton.figma.tsx` | `2210:302` | 2026-05-19 | Single mapping. No Figma component properties (visual is icon-only and the icon is baked into the master). Example uses `onClick={() => {}}` placeholder so Dev Mode shows the polymorphic `<button>` form with the `--interactive` modifier active. |
| FormField | `packages/ui/src/FormField.figma.tsx` | `2255:98` | 2026-05-20 (awaiting `npm run connect:publish`) | Single mapping. `Label` TEXT → `label`, `Placeholder` TEXT → `placeholder`, `Show icon` BOOLEAN + `Icon` INSTANCE_SWAP → `trailingIcon` slot (gated by figma.boolean true/false), `State` enum → `error` (Default/Focus → undefined, Error → `"Invalid input"` example). |
| AuthModal | `packages/ui/src/AuthModal.figma.tsx` | `2244:1373` | 2026-05-20 (awaiting `npm run connect:publish`) | Single mapping. `children` mapped via `figma.children('Content')`. Header (OdysseyLogo dark) is baked into the master and not exposed as a prop. |
| AuthContent | `packages/ui/src/AuthContent.figma.tsx` | `2264:712` | 2026-05-20 (awaiting `npm run connect:publish`) | Single example mapping — props are internal-state-driven so no component-property bindings; consumer-facing API is callbacks (`onLogin`, `onForgotPassword`, `onCreateAccount`). Will be published in the same sync. |

## Icon Tracker

Lucide icons needed in the Figma library are tracked in `playground/icon-tracker.html`. Open in a browser to see pending vs done icons with their config (plugin search name, target Figma name, size, stroke). Updated by the `/normalize` routine's Step 8b whenever a normalized component touches a new icon.

*Updated 2026-05-12:* User added 2 new icon masters this session ahead of the WidgetsLeftMenu + SearchField normalize cycle:
- `lucide/search` (`1948:848`) in **Icons lg** frame — used by SearchField (and any downstream search consumers).
- `lucide/grip-vertical` (`1948:910`) in **Icons md** frame — used by WidgetsLeftMenu item rows (hover-revealed drag affordance) and available for any future draggable-list patterns.

---

## Purge Notes

When running a purge routine:
1. Read this file first
2. **SKIP** anything listed in "Ad-hoc Implementations" — those still need their inline styles
3. Only remove old patterns from components listed in "Normalized Components"
4. Check "Composite Text Utilities" — if a utility is listed, it's in use and must not be removed

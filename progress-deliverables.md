# Odyssey — Deliverables Progress

> Parallel to `progress.md` (the prototype build log). This file tracks **deliverable work only** — story packs, dev-facing guides, PM-facing artifacts. Separate log so deliverable sessions and prototype sessions can run as independent agent threads. Sessions numbered D1, D2, …

## Session D1 — August 20–21, 2026 (extracted from prototype S129)

- **Story pack for Kathleen (`docs/story-packs/spotbid-2026-08-21/`).** Her question was whether the whole project should be sent and whether she'd have to take her own screenshots — no to both. The pack carries the model in a paragraph, **28 built behaviors across both surfaces each tagged with provenance** (her PRD/calls · SPB-NN · Jira prior art · *ours, needs ratification*), **18 story skeletons with Given/When/Then AC**, a **stand-ins list** so prototype shortcuts (email, auth, OCM profiles, fuel, history) can't harden into requirements, a trace table back to her own 8 PRD stories, and **18 named screenshots** captured live against Neon and keyed to the stories so she attaches rather than captures. Scope is deliberately the two built surfaces — SpotBid tab + bid entry page — matching the `SPB-43` V1 list; the carrier board and monitoring board are listed as consciously deferred.
- **Open questions on the pack (v1):** whether it also lands in Confluence/Jira (MCP is connected) · whether a provision-only carrier-board appendix is added · what devs specifically need beyond AC + screens (component/token references, API contract shape).
- **New direction (user, Aug 23):** Kathleen prefers stories NOT name components — that's stretching them into the *how*, and she's right. Devs still need a component guide, so the guide moves out of the stories and into the prototype itself: a toggleable **dev mode** (URL param + floating draggable toggle) that overlays each component's React + Angular name on the live UI without blocking interaction.

## Session D2 — August 23, 2026

**DEV MODE SHIPPED (local) — the component guide became a feature of the prototype itself.** Plan `docs/superpowers/plans/2026-08-23-devmode-component-overlay.md` executed subagent-driven (Sonnet implementers, per-task spec + quality review): `?dev=1` activates; a draggable corner-snapping toggle (flyout: Hover/All mode, React/Angular mapping) drives an overlay that labels every outermost `@odyssey/ui` component; chips carry name · tier · version + NORMALIZING dot; chip click opens a detail modal with both framework names, versions, the props table from the live DSM metas, and DSM links (Angular link disabled until the Angular DSM is published). Tests 1606 → 1668.

- **Zero component edits.** Identification walks React's fiber tree (`__reactFiber$` keys) by component **identity**, not name — a quality reviewer caught that name-matching would have died in the minified prod bundle, exactly where devs will use it. No `@odyssey/ui` change, no NORMALIZING demotions, no Angular library debt.
- **Angular names are generated, not typed.** `tools/gen-angular-names.mjs` parses the sibling repo's 77 DSM demo metas → committed `angular-map.json` (77/77 mapped; IconButton skipped as deprecated). Two data findings settled by reading the repos: `SearchField` is a deprecated alias of `ComboBox` (explicit `DEPRECATED_ALIASES` handling — namespace enumeration is alphabetical, so first-wins was luck); the joint "CalendarPicker + DatePicker" meta splits into two entries with selectors verified against the Angular lib source.
- **Review gates caught 9 real defects pre-QA** — headline three: prod-minification name matching, detached-element chips piling at (0,0) after route changes, and the flyout's Escape double-closing product modals through the S119 escape stack.
- **Browser QA: 12/12 PASS** in real headless Chrome (hover + all modes, click-through, drag/persist all 4 corners, flyout on-screen from every corner, modal with real props, framework flip, external bid page, chip clamping on a scrolled container, zero console errors). The overlay also skips devmode's own chrome (`data-devmode` guard) so the inspector can't inspect itself.
- **Open / next:** Task 7 — publish the Angular DSM as its own Vercel project (`odyssey-dsm-angular-stage`, CLI deploy from `odyssey-one-library-ui`, no push/npm) and fill `ANGULAR_DSM_URL`; **user-gated, not started**. Prototype redeploy needed before devs can use dev mode on the live URL. Known ceilings (named in code): all-mode is blank after route change until a scroll; chip clamp is top-left only.

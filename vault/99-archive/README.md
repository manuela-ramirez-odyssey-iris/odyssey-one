---
title: Archive
type: meta
status: archived
---

# 99 — Archive

Parked, superseded, or otherwise out-of-active-rotation content. Kept for historical reference, not for active work.

## Contents

- `first-prototype/` — pre-React baseline (HTML/CSS/JS prototype + design.md). `node_modules/` NOT included (reproducible from package.json).
- `code-reference/` — old HTML reference files (e.g. `login.html`)
- `screenshots-unsorted/` — generic error*.png / issue.png screenshots whose context was lost. Re-tag and move into their domain folder when context surfaces.
